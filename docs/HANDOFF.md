date | type | observed | impact | decision | evidence | status
2026-08-20 | platform-adapter-gap | The installed PS1 localization adapter is explicitly skeletal and has no PS1 project initializer/extract contract. | Its generic project harness cannot be used as a PS1 completion gate. | Preserve the established `_work/tools` and `_work/records` layout; apply the retro-patch investigation invariants and record every target-specific contract here. | `game-translate/platforms/ps1/PLATFORM.md`; project initializer help | open
2026-08-20 | extraction-baseline | Existing extractor intentionally omits STR and DA media. All 39 listed non-media extracted files byte-match Track 1. | Text/graphics coverage is not complete until media is classified. | Treat STR/DA as unclassified, not out of scope. | `records/iso-listing.txt`; 2026-08-20 source-to-extract hash comparison | open
2026-08-20 | compression | Four LZB files decompress to EOF with the existing decoder; no encoder or unchanged round-trip exists. | Any compressed-file reinsertion is blocked. | Use decompression only for analysis; do not modify or reinsert LZB assets. | `_work/tools/lzb.py`; initial survey | open
2026-08-20 | loader-map | The executable's 37-entry path-pointer table and three CD read entry points were mapped, including STAYDAT at file index 28. | Static file consumers can now be named by stable file index. | Treat `ARCHITECTURE.md` and `analyze_structures.py` as the reproducible target-specific contract. | EXE file offset `0x20C10`; RAM `0x800C2BC0`, `0x800C2F78`, `0x800C3158` | verified-static
2026-08-20 | staydat-pack | Sixteen aligned STAYDAT slices are byte-identical to their separate DAT files and map from RAM `0x80020000`. | Padding garbage and developer fragments must not be counted as user-facing text. | Analyze named slices only unless a runtime consumer proves a padding region live. | `_work/tools/analyze_structures.py`; 16/16 byte comparisons | verified-static
2026-08-20 | dnames-inventory | D_NAMES has 17 active absolute-pointer sections, including four alias subtables, and 4,090 FF-terminated entries. | Stable raw IDs now exist, but readable extraction is blocked on token/charmap proof. | Preserve raw bytes and FF; do not infer F0-FE token lengths or translate yet. | `_work/analysis/dnames_raw_manifest.tsv`; `TEXT_MAP.tsv` | inventory-closed_decode-open
2026-08-20 | mbanks-coverage | M_BANKS has a 61-entry top header; 51 candidate subtables expose 11,530 FF-terminated message candidates, five entries are null, and five remain structurally special. | Dialogue population is not yet complete. | Do not issue a translation manifest until all non-null header entries have consumer-backed boundaries. | `_work/tools/analyze_structures.py`; `ARCHITECTURE.md` | open
2026-08-20 | glyph-decoder | Two MAP consumers implement 00-EF single-byte glyph IDs and F0-F5 plus trail as pages 01-06; F6-FE enter control dispatch and FF terminates. | Raw strings can now be segmented at glyph level, but not mapped to Japanese or safely reinserted across controls. | Preserve F0-F5 pairs and all F6-FE bytes; resolve control arity and glyph-ID-to-character/font mapping next. | `ENCODING.md`; MAP decoded offsets `0xB3F0` and `0x4D378` | verified-static
2026-08-20 | emulator-smoke | A separate hidden DuckStation batch process accepted the Rev 1 CUE, remained responsive, and exposed the Japanese game title; hidden D3D PrintWindow capture was black. | This is boot-process evidence only, not visual/runtime text evidence. | Stop the test process and keep visual QA blocked until an emulator-native screenshot or visible capture is available. | DuckStation process smoke; `_work/analysis/emulator/baseline-hidden.png` | bench-only_visual-open
2026-08-20 | structural-ff-correction | Control-aware parsing showed 308 of 4,090 D_NAMES records contain FF inside an F0-F5 trail or control operand before the true terminator. | The first raw manifest truncated those 308 records even though IDs and pointers were correct. | Adopt control arity F6:0,F7:0,F8:1,F9:1,FA:0,FB:2,FC:2,FD:2,FE:1 and regenerate the raw manifest; never use bytes.find(FF). | `analyze_structures.py`; `ENCODING.md`; Complete Box commit `9883a43e...` | corrected-and-verified
2026-08-20 | cross-version-charmap | The Complete Box reviewed glyph dictionary decodes SRW4S D_NAMES into coherent robot, pilot, weapon, terrain, scenario, and song names. | 960/969 used glyph IDs have donor characters; remaining low/structural ambiguities still block an authoritative target charmap. | Keep decoded Unicode derived, pin donor commit/hash, apply only evidence-backed target overrides, and preserve raw/glyph IDs as authority. | `_work/analysis/dnames_decoded_candidate.tsv`; donor mapping SHA-256 `791bb9ff...` | candidate-high-coverage
2026-08-20 | mbanks-runtime-contract-correction | MAP loads exactly 0x200 bytes for every one of 61 header entries and its selector consumes a fixed 256-entry u16 cache, then reads 0x400 bytes from `(header[bank] & 0xFFFF0000) + pointer[slot]`. | The prior variable-table inference of 51 tables and 11,530 FF-terminated messages was structurally wrong and is superseded; 56 non-null tables expose 14,336 candidate runtime slots: 14,105 token-terminated, 180 in-file without a terminator in the window, and 51 out-of-file unused candidates. | Inventory all fixed slots, bound structural parsing to the 0x400 runtime read, allow records to cross 64 KiB boundaries, and do not call slots live dialogue until selector references are collected. | MAP decoded offsets `0x4DB28`, `0x4DD00`; `_work/analysis/structure_report.json`; `_work/analysis/mbanks_candidate_manifest.tsv` | corrected-runtime-proven_live-population-open
2026-08-20 | mbanks-selector | The MAP interpreter routes values at or above 0x9000 to M_BANKS using `bank=(selector-0x9000)>>8` and `slot=selector&0xFF`; a separate structure consumer loads the high and low bytes of a u16 field at record offset +6 into the same function. | Every candidate slot now has an exact reversible selector, but byte-pattern scans alone cannot prove a selector live. | Add `selector_hex = 0x9000 + bank*0x100 + slot` to the manifest; require a proven script/record field before promoting a selector to live. | MAP decoded offsets `0xA130..0xA1B4`, `0x4D0A0..0x4D164` | verified-static_live-reference-open
2026-08-20 | target-native-font | MAP dereferences a descriptor at RAM 0x80056800; its first three pointers select contiguous STAYDAT glyph banks at file offsets 0x36838, 0x37838, and 0x3F838, ending at 0x43838. | The main target font is no longer blocked: 1,792 native glyph bitmaps and exact 8x16/16x16 1bpp formats are known. Full KS X 1001 replacement cannot fit the 1,776 encodable IDs without a subset or engine expansion. | Treat STAYDAT 0x36838..0x43837 as the authoritative font span, preserve the descriptor, render with `render_target_font.py`, and calculate the final Korean subset before allocating slots. | `FONT.md`; `_work/analysis/font_descriptor/*.png`; MAP decoded offsets `0xAB20`, `0xAF1C`, `0xACC4` | verified-static_allocation-open
2026-08-20 | target-charmap-review | Target-native bitmap review plus repeated Japanese context resolves 14 previously ambiguous values, including 兜, 汎, 桜, 魂, 遷, 閥, 慕, 愾, 毅, 候, 惨, 逐, ▲, and blank glyph 3FF. | D_NAMES unresolved coverage falls from 8 glyph types/104 occurrences to three icon-like types/54 occurrences; raw authority is unchanged. | Keep only jointly bitmap/context-supported overrides; leave 00C, 0EA, and 0ED unresolved instead of forcing Unicode interpretations. | `render_target_font.py`; `_work/analysis/font_descriptor/unresolved_glyphs.png`; regenerated `dnames_decoded_candidate.tsv` | candidate-high-coverage_3-icons-open
2026-08-20 | text-token-roundtrip | The lossless codec exhaustively round-trips all 1,776 representable glyph IDs, 4,090 D_NAMES occurrences, and 14,105 token-terminated M_BANKS runtime candidates with zero byte mismatches. | Token extraction/re-encoding is no longer a blocker, but relocation, translated growth, live-slot coverage, and disc reinsertion remain unproven. | Pin raw token bytes and control operands, exclude out-of-file/nonterminated slots, and use `text_roundtrip_report.json` as the pre-translation codec gate. | `tools/text_codec.py`; `tools/verify_text_roundtrip.py`; `_work/analysis/text_roundtrip_report.json` | verified-static_relocation-open
2026-08-20 | mbanks-event-vm-live-catalog | MAP's 8-byte event opcode table proves C4-CC message-field writers; SC_00/04/05 entry/fallthrough walks expose 1,584 unique operand sites and 1,550 unique M_BANKS targets after clearing record state flag bit 15. Direct/formula consumers add 74 targets, yielding a 1,624-target consumer-bounded set. Control opcodes 01/02/03/04/60 prove jump/call/return/source-switch behavior; the SC_00 linear over-approximation contains 141 transfer sites, including 73 targets outside that source. | The candidate population is reduced from 14,336 slots, but handler-directed CFG edges, root reachability, and dynamically selected sources are not yet complete; some linear fallthrough references may be false positives. | Use `mbanks_live_selector_refs.tsv` and `mbanks_live_closure.tsv` as the current review scope; never label it final live coverage until CFG/dynamic-root closure is proven. | `_work/tools/analyze_live_selectors.py`; `_work/analysis/mbanks_live_selector_report.json`; MAP `0x274/0x278`, `0xDC94/0xDCB8`, `0x4D164` | verified-static_consumer-bounded_full-cfg-open
2026-08-20 | mbanks-fb-and-font-baseline | Text control FB forms a little-endian selector and statically re-enters M_BANKS at values >=0x9000; recursive inspection adds no targets to the current closure and finds no FF dynamic substitution in those targets. The 1,624 targets use 1,142 source glyph IDs; union with D_NAMES is 1,316. | Preserving every currently used source glyph leaves a conservative 460 of 1,776 encodable IDs, but this does not measure Korean glyph demand. | Preserve FB operands, keep dynamic substitution fail-closed, and calculate the final Korean subset only from an approved translation corpus and explicit source-glyph retention policy. | `_work/analysis/mbanks_live_selector_report.json`; `ENCODING.md`; `FONT.md` | verified-static_allocation-baseline_translation-open
2026-08-20 | sc01-03-stage-window-gate | File indices 22-24 read stage-selected 0x800-byte windows through RAM 0x80010000 and copy them to 0x80080800/0x80081000/0x80081800; opcode 60 source cases 1/2/3 switch the VM cursor to those bases. Treating all 80 per-file offsets as reachable roots produces invalid M_BANKS banks in SC_02/03. | The source-switch linkage is proven, but file membership alone cannot promote every stage window to live VM code; doing so contaminates the message set with false positives. | Keep SC_01-03 outside the current 1,624-target closure until stage/root reachability is derived from the selectors that populate the stage index; retain the failed broad scan as a rejected inference, not a result. | MAP `0x161C8/0x16298/0x16368`; globals `0x8015F344..0x8015F34C`; opcode 60 handler `0x8011A510` | verified-linkage_root-reachability-open
2026-08-22 | lzb-encoder-unblocked | 이 타깃의 LZB는 컴플리트 박스 프로젝트(SLPS-020.70 RAM `0x8004e548`)의 LZ와 **동일 포맷**이다. 두 독립 디코더가 4개 파일에서 바이트 동일한 결과를 내고 각각 입력 끝까지 소비한다. 그 저장소의 인코더로 해제물을 재압축하면 해제 결과가 원본 해제물과 바이트 동일하다. | "압축 파일 재삽입 불가" 판정이 해제된다. 다만 재압축본이 원본보다 0.1~0.5% 크다(MAP +585B, BATTLE +430B, M_BANKB +179B, MOVIE +28B). | `tools/lzb.py`(자체 이식)와 도너 `srw_lz.py`를 교차 검증기로 유지하고, 압축 파일 수정 시 크기 증가분은 재배치 예산으로 계산한다. 아직 게임 런타임에서 재압축본을 소비시켜 보지는 않았다. | `tools/verify_lzb_roundtrip.py`; `analysis/lzb_roundtrip_report.json`; MIPS `0x800C345C` | verified-static_runtime-consumption-open
2026-08-22 | image-roundtrip | Track 1의 `DAT/STAYDAT.BIN` 249섹터를 원본과 같은 내용으로 다시 쓴 무변경 왕복 이미지가 원본과 SHA-256 바이트 동일하다(`963108980bc28d34...`). | MODE2 Form 1 EDC/ECC 재계산이 원본 보호 필드를 정확히 복원한다. 크기 보존 in-place 교체 경로가 열렸다. | `tools/patch_image.py`를 유일한 이미지 쓰기 경로로 쓰고 Expected Write(대상 LBA 현재 내용 == 원본 추출본)를 게이트로 유지한다. 파일 성장·재배치는 아직 별도 게이트다. | `tools/patch_image.py`; 도너 `patch_raw_track_exes.rebuild_mode2_form1` | verified-byte-identical
2026-08-22 | visibility-poc-pass | 갈무리14 한글 512자를 글리프 ID `0x100–0x2FF`에 주입하고 `D_NAMES` 14개 레코드를 재인코딩한 패치 이미지가 DuckStation에서 부팅됐고, 주인공 설정·이름 입력·대사창·맵 메뉴·유닛 목록에서 한글이 정상 렌더된다. | 되돌리기 비싼 작업(전량 번역, 전체 폰트 생성, 엔진 본 패치)을 막고 있던 가시성 게이트가 통과했다. 8×16 low bank는 무손상이라 가나·ASCII는 그대로다. | `records/POC.md`를 통과 기록으로 삼고, 재인코딩 **문자열**의 화면 표시는 다음 런타임 확인 항목으로 남긴다. | `analysis/emulator/PASS_glyph_visibility.png`; `analysis/poc_build_report.json` | passed
2026-08-22 | hangul-demand-estimate | 같은 시리즈·같은 엔진 계열인 컴플리트 박스 한국어 번역 **전체**(제2차+제3차+EX, 번역 문자열 약 10만 건)가 쓰는 고유 한글 음절은 **1,143자**다. | SRW4S의 16×16 슬롯 예산 1,536개(ID `0x100–0x6FF`) 안에 든다. 즉 **글리프 페이지 확장이나 조합형 없이** 완성형 한글로 전량 번역할 수 있을 가능성이 높다. | 이 수치는 도너 코퍼스의 관측치이지 SRW4S의 사실이 아니다. 최종 할당표는 승인된 한국어 코퍼스에서 다시 계산하고, 보존할 원문 기호·아이콘 글리프를 먼저 고정한 뒤 확정한다. | `reference/srwcb-korean-patch/translation/*.json` 집계; `FONT.md` | estimate-from-donor_target-recount-open
2026-08-22 | runtime-harness | emucap Mednafen 어댑터가 이 호스트에 없어(상류 문서상 Windows 빌드 BETA·비자명) DuckStation을 런타임 검증 경로로 채택했다. 방향키에 `KEYEVENTF_EXTENDEDKEY`가 없으면 스캔코드가 숫자패드로 잡혀 DuckStation 바인딩에 도달하지 않는다. | 자동 조작·캡처 경로가 확보됐다. 메모리 읽기/브레이크포인트는 이 경로로 얻지 못하므로 M_BANKS CFG closure 같은 동적 증거는 여전히 정적 분석에 의존한다. | `tools/ds_drive.py`·`tools/ds_ctl.py`를 표준 도구로 삼고, 1화 맵 세이브스테이트(`SLPS-00196_1.sav`)로 재검증 비용을 줄인다. 동적 증거가 필요해지면 Mednafen 어댑터 빌드를 별도 과제로 연다. | `records/POC.md` | adopted_dynamic-debug-open
2026-08-22 | dnames-banking | MAP 해제물 `0x4E4A8`(RAM `0x80154828`)의 D_NAMES 접근자는 문자열 주소를 `base + (header[section] & 0xFFFF0000) + u16`으로 만든다. 현재 18개 헤더 값이 모두 `<0x10000`이라 한 창에 몰려 있을 뿐, 엔진은 이미 **섹션별 64KiB 뱅킹**을 지원한다. | "16-bit 포인터 천장 65,536"이 D_NAMES의 상한이라는 판단이 뒤집힌다. 포인터 폭 확장이나 엔진 패치 없이 여러 64KiB 창으로 재배치할 수 있다. | 재배치기는 헤더 u32를 `(창<<16)|표오프셋`으로 쓰고, 별칭 그룹(S04+S05 / S08+S09+S10 / S13+S16)을 같은 창에 묶는다. blob마다 규약이 다르므로(D_OBJCT는 표 상대, 뱅킹 없음) 다른 자산에 이식하지 않는다. | `records/REINSERTION.md` §2; capstone 디스어셈블 | verified-static
2026-08-22 | growth-measured | 도너(컴플리트 박스) 한국어 번역을 우리 D_NAMES 일본어 파생 보기에 매칭해 881개 레코드(33.2%)를 실측했다. 5,545 B → 8,734 B = **1.575배**. 가타카나가 1바이트/글자인데 한글 음절은 2바이트라 이름 구간이 가장 크게 는다. | 현재 STAYDAT 슬롯 53,248 B 안에는 번역본이 들어가지 않는다. 전체 투영 약 74,000 B이지만 산문 4개 섹션(문자열의 70%)은 표본이 15~62 B뿐이라 오차가 크다. | 재배치를 전제로 진행하되 최종 수치는 승인된 한국어 코퍼스에서 다시 낸다. 투영값을 확정 사실로 쓰지 않는다. | `tools/measure_growth.py`; `analysis/dnames_growth_measured.json` | measured-partial_corpus-recount-open
2026-08-22 | dnames-reference-map | D_NAMES 4,090 엔트리 / 고유 대상 2,653개 중 **공유 대상 566개**(최다 192회)와 **레코드 안쪽 앵커 13개**(S13/S16, +2/+4/+5)를 찾았다. 표·레코드가 덮지 않는 3,841 B가 2,596 조각으로 흩어져 있고 대부분 실제 텍스트다. | 앵커 13개는 도너가 v0.11.34/35에서 프리즈를 일으킨 것과 같은 모양이다. 미덮임 조각은 도너의 "비참조 레코드"(3게임 160곳)와 같은 모양이라 빈 공간이 아니다. | 재배치기는 레코드 시작 + 내부 앵커 오프셋을 함께 옮기고, 미덮임 조각은 회수하지 않고 그대로 옮긴다. | `tools/analyze_growth.py`; `analysis/dnames_growth_report.json`; 도너 릴리스 노트 v0.11.30/34/35 | verified-static
2026-08-22 | mbanks-growth-policy | M_BANKS는 물리 64KiB 창 8개에 56뱅크·14,336슬롯이고 창 여유는 합계 42,748 B(8.7%)뿐이다. 실측 최장 레코드 1,023 B로 `0x400` 읽기 창을 넘는 레코드는 0개다. | 헤더가 u32이고 파일이 CD에서 오프셋으로 스트리밍되므로, 새 64KiB 창을 파일 끝에 덧붙이고 헤더만 갱신하면 된다. 엔진 패치 불필요. | 레코드 상한 1,023 B를 번역 검사 게이트로 넣는다. 파일 성장분은 디스크 재배치 예산으로 넘긴다. | `records/REINSERTION.md` §4 | verified-static
2026-08-22 | disc-budget | Track 1 꼬리 여유는 150섹터(307,200 B)뿐이고 파일 사이 빈틈은 0이다. `NULL.DA`(27 MB)·`CMSONG.DA`는 오디오 트랙 영역이라 도너가 쓴 트레일링 null 수법을 그대로 쓸 수 없다(이 디스크의 트랙 2·3은 실제 CD-DA). | 투영 증가분(M_BANKS 약 +250 KB, D_NAMES 약 +38 KB)이 꼬리 여유를 넘는다. | cue가 트랙별로 파일이 분리돼 있으므로 Track 1 .bin 뒤에 섹터를 덧붙이는 경로를 별도 게이트로 증명한다(ISO의 DA 파일 LBA·볼륨 크기·cue 트랙 시작 갱신 포함). | `records/REINSERTION.md` §5 | open
2026-08-22 | mednafen-windows-build | emucap Mednafen 어댑터를 이 호스트에 빌드하려면 MSYS2/MinGW 외에 네 가지 우회가 필요했다: (1) build-lock의 `ps -o lstart=`가 MSYS2에 없어 `/proc/<pid>/stat` starttime을 돌려주는 ps 셔임, (2) `_common/test-native-failure.sh`의 `-Werror`가 GCC 16의 unused-parameter에 걸림, (3) 자기시험이 `/tmp`를 네이티브 경로로 못 봐 `TMPDIR`을 Windows 경로로 지정, (4) `mingw_app_type` 심볼이 현재 mingw-w64 CRT에 없어 링크 실패. 추가로 FLAC dll 데이터 자동 import가 `-mcmodel=small -fno-pic`와 충돌해 pseudo-reloc 런타임 실패, `UNICODE` 미정의로 "Windows 98 전용 빌드" 모달이 뜬다. | 상류 소스를 수정하지 않고 래퍼·env만으로 넘긴다. | 셔임은 `%TEMP%\claude\...\scratchpad\psshim`(ps, cxxwrap, mingw_app_type.o), 정적 라이브러리는 같은 곳 `staticlibs`(FLAC/ogg/z/intl/iconv)에 둔다. 빌드는 `CXX=cxxwrap CPPFLAGS='-DUNICODE -D_UNICODE -DFLAC__NO_DLL' LDFLAGS='-L<staticlibs> -static-libgcc -static-libstdc++' LIBS=-logg TMPDIR=<win path>`로 실행한다. | 빌드 로그; `adapters/mednafen/README.md`의 Windows BETA 경고 | in-progress
2026-08-23 | mednafen-adapter-live | emucap Mednafen 어댑터가 이 호스트에서 실제로 붙었다(read/write memory, BP, screenshot, savestate 동작). 실행 조건: HOME 기반 base dir에 `firmware/scph5500.bin`, `-video.driver softfb`(dummy만으로는 프레임 루프 미도달), 콘텐츠 경로는 ASCII(한글 경로면 핸드셰이크가 UTF-8 오류로 깨짐). | DuckStation으로 못 얻던 메모리·브레이크포인트 증거가 열렸다. | 실행은 `scratchpad/run_mf.sh`, 디스크는 정션 `D:\srw4s_poc`를 쓴다. | `records/RUNTIME_CHECKS.md` §재현 메모 | verified-live
2026-08-23 | bp-memory-type-trap | `write` 브레이크포인트를 `memory_type:"ram"`(물리 오프셋)으로 걸면 **한 번도 발화하지 않는다**. `memory_type:"cpu"` + KSEG0(`0x80xxxxxx`)이어야 발화한다. 스택 영역 보정에서 ram=0건 / cpu=4,096건으로 갈렸다. | 보정 없이 얻은 "쓰기 없음"은 전부 거짓 음성이다. 실제로 이 함정에 한 번 빠져 D_NAMES·후보 대역 결과를 폐기하고 다시 쟀다. | 모든 BP 실험은 먼저 확실히 쓰이는 주소(스택)로 보정한다. `read_memory`/`find_pattern`은 `ram`이 정상이므로 BP만 규약이 다르다는 점을 기억한다. `poll_events` 큐 잔여도 BP 교체 후 count=0까지 드레인한다. | `records/RUNTIME_CHECKS.md` §0 | verified-live
2026-08-23 | battle-flat-accessor | BATTLE.LZB는 MAP과 **같은 주소 `0x80106380`에 교체 적재**된다(find_pattern으로 확정). 그 안의 D_NAMES 접근자(RAM `0x80146EC4`)에는 `& 0xFFFF0000`이 없어 문자열을 `base + u16`으로 평탄하게 푼다. | `D_NAMES`의 실제 상한은 65,536 B다. "엔진 패치 없이 섹션별 64KiB 뱅킹" 결론은 MAP만 본 것이었고 정정한다. 뱅킹을 쓰려면 BATTLE 접근자에 같은 마스킹을 넣는 ASM 개입이 필수다. | 재배치 설계는 ①64KiB 이내로 압축하거나 ②BATTLE 접근자를 패치하거나 둘 중 하나를 명시적으로 고른다. | `records/RUNTIME_CHECKS.md` §1; `REINSERTION.md` §2 정정 | verified-static+live
2026-08-23 | dnames-read-only | 보정된 `cpu` write BP를 D_NAMES 전 구간(`0x80069800–0x80076287`)에 걸고 주인공 설정 이후 맵·대사·유닛 상태·턴 종료·적 페이즈·전투 애니메이션까지 4,800프레임 이상 돌렸다. 쓰기 0건. | 주인공 이름은 D_NAMES 레코드가 아니라 별도 버퍼에 있고 대사에서 `FB` 셀렉터로 끌어온다. 재배치기가 런타임 쓰기 주소를 따라갈 필요는 없다. | 이름 입력 화면은 이번 감시 구간에 없었으므로 완전 배제는 아니다. 그 화면을 포함한 재측정을 남긴다. | `records/RUNTIME_CHECKS.md` §3 | verified-live_name-entry-open
2026-08-23 | ram-relocation-candidate | STAYDAT 바로 위(`0x800A0000`·`0x800A8000`)는 사용 중이다. 오버레이 위 구간에서 쓰기는 `0x8018A284`와 `0x801A4000–0x801A87BE`에 몰려 있고, **`0x80166000–0x80189FFF`(144 KiB)는 4,800프레임 동안 쓰기 0건**이었다. | D_NAMES 재배치 후보 구간이 생겼다(필요량 약 90 KB). | 1화 한 장면의 관측이므로 인터미션·다른 시나리오·전투 연출을 더 돌려 확인하기 전에는 "비어 있음"으로 확정하지 않는다. | `records/RUNTIME_CHECKS.md` §2 | candidate_more-scenes-open
2026-08-23 | lzb-format-confirmed-live | RAM `0x80106380`의 MAP 오버레이가 우리 `MAP.dec`와 헤더 16바이트 및 스트림 중간 `0x30000` 지점 128바이트까지 바이트 동일했다. | 우리 **해제기**가 게임 자신의 해제 결과를 정확히 재현한다. | 남은 것은 우리 **인코더** 출력의 런타임 소비다. 레벨 16으로도 `MAP.LZB`가 `+369 B`라 제자리 교체가 안 되므로 ISO 재배치 게이트를 먼저 연 뒤 확인한다. | `records/RUNTIME_CHECKS.md` §4 | decoder-verified_encoder-open
2026-08-23 | battle-accessor-patched | BATTLE의 D_NAMES 접근자를 MAP과 같은 64KiB 뱅킹으로 고쳤다. 13워드가 필요한데 원본 자리는 11워드뿐이라 `0x80146EC0`에 `j 0x80164FB8`+`nop` 트램폴린을 두고, 본체를 BATTLE 해제물의 정적 참조 0건인 0-run(`0x80164FB8`, 99 B)에 넣었다. 재압축본은 원래 96섹터 안에 741 B 여유로 들어가 LBA 이동 없이 디렉터리 레코드 크기만 갱신했다. | 엔진이 뱅킹을 받아들인다. 모든 헤더가 아직 `<0x10000`이라 동작은 원본과 동일해야 하고 실제로 동일했다(순수 회귀 시험). | 기계어는 capstone 역디코드 대조·load-delay 검사·Expected Write 2건·`j` 영역 검사·재압축 왕복을 모두 통과시켰다. 상세는 `records/BATTLE_ACCESSOR_PATCH.md`. | RAM `0x80164FB8` 52바이트가 빌드물과 바이트 동일, exec BP가 전투 중 반복 히트(`a0=7` 파일럿 약칭), `analysis/emulator/PASS_battle_accessor.png` | verified-live
2026-08-23 | lzb-encoder-runtime-proven | 위 이미지에는 **우리 인코더가 만든 `BATTLE.LZB`**가 들어 있고 게임이 그것을 읽어 해제해 실행했다(전투 정상). | `RUNTIME_CHECKS.md` §4의 마지막 미확인 항목이 닫혔다. 해제기는 이미 RAM 대조로 확인됐으므로 LZB 왕복 전 구간이 실기로 증명됐다. | 압축 파일 수정은 이제 "섹터 예산 안에 드는가"만 확인하면 된다. | `records/BATTLE_ACCESSOR_PATCH.md`; `analysis/battle_accessor_patch.json` | verified-live
2026-08-23 | image-resize-same-sector | 같은 섹터 수 안에서 파일이 커지는 교체 경로를 만들었다(`tools/patch_image_resize.py`) — LBA 고정, ISO 디렉터리 레코드 data length(LE/BE) 갱신, 해당 섹터 EDC/ECC 재계산. 무변경 왕복이 원본과 SHA-256 바이트 동일함을 먼저 확인했다. | 섹터 여유 안에서 커지는 파일은 재배치 게이트 없이 반영할 수 있다(BATTLE.LZB가 첫 사례). | 섹터 수가 달라지면 실패시킨다. 진짜 재배치는 여전히 별도 게이트. | 무변경 왕복 SHA-256 일치 | verified-byte-identical
2026-08-23 | staydat-packed | D_NAMES 베이스에서 도달 가능한 창 0/1/2(`0x80069800`·`0x80079800`·`0x80089800`)는 전부 STAYDAT 안이고 **512 B 이상 0-run이 한 곳도 없다**. STAYDAT 전체로 넓혀도 256 B 이상 0-run은 2곳 1,289 B(0.3%)뿐이다. STAYDAT 바로 위 RAM(`0x800A0000`)도 사용 중이라 파일을 그냥 키울 수 없다. | 접근자는 뱅킹을 받아들이지만 **데이터를 놓을 자리가 없다**. 뱅킹 실사용은 STAYDAT 재배치가 선행되어야 한다. | 창 0–2가 쓸 192 KiB를 D_NAMES 전용으로 비우고 그 안의 다른 슬라이스를 옮긴 뒤 베이스 표(MAP `0x8015F2E8~`, BATTLE 사본) u32 슬롯을 갱신하는 재배치 설계를 다음 과제로 연다. | `records/BATTLE_ACCESSOR_PATCH.md` §남은 것 | open
2026-08-23 | dnames-projection-corrected | S00/S01은 산문이 아니라 **창 그리기 스크립트**다(프레임 타일로 쓰이는 글리프 ID + 제어 토큰). 번역 대상은 위치 토큰 `FC`/`FD` 뒤의 라벨 span뿐이라 S00은 바이트의 22.3%, S01은 25.2%, S02는 48.5%만 번역된다. 도너 치환 span 733건(7,564 B)으로 잰 span 배율은 **1.28–1.39배**(이름 레코드의 1.575배와 다름). | `D_NAMES` 투영이 74,000 B에서 **64,349 B**로 내려간다 — 16-bit 창 65,536 B 안에 여유 1,187 B(1.8%)로 들어간다. **뱅킹은 아마 필요 없다.** 다만 현재 STAYDAT 슬롯 53,248 B보다 +11,101 B 더 필요하다. | 여유가 1.8%뿐이므로 승인된 한국어 코퍼스가 나오기 전에는 확정하지 않는다. BATTLE 접근자 패치는 여유가 사라질 경우의 보험으로 유지한다. | `records/SPACE_BUDGET.md`; `tools/span_classify.py` | measured-model_corpus-confirm-open
2026-08-23 | standalone-dnames-dead | STAYDAT 사본만 한국어로 패치하고 독립 `DAT/D_NAMES.BIN`은 원본으로 둔 이미지를 부팅해 RAM `0x800740CD`를 읽으니 한국어 바이트(`F0 02 F0 03 FF`)가 나왔다. | 게임이 읽는 것은 **STAYDAT 사본**이고 독립 파일은 죽은 중복본이다. 독립 파일을 키워 공간을 얻는 길은 막혔다. | 커진 D_NAMES를 다른 곳에 두려면 로더 훅으로 별도 적재해야 한다(후보: 검증된 빈 대역 `0x80166000–0x80189FFF`, 144 KiB). | `records/SPACE_BUDGET.md` §4 | verified-live
2026-08-23 | staydat-shift-unsafe | D_NAMES 뒤 STAYDAT 영역(`0x80056288`–`0x8009C168`)을 겨누는 하드코딩 상수가 65곳이다(MAP 53, BATTLE 6, EXE 6). | 베이스 표만 고쳐서 슬라이스를 미는 재배치는 안전하지 않고, `jalr`/계산 주소 때문에 열거의 완전성도 보장 못 한다. | STAYDAT 안에서 밀어 공간을 만드는 안은 기각한다. | `records/SPACE_BUDGET.md` §4 | rejected-with-evidence
2026-08-23 | disc-reloc-gate-passed | 트랙 1 꼬리 LBA 166,581–166,730의 150섹터가 mode 2 / submode 0 / 전부 0인 진짜 패딩임을 확인하고, 그리로 파일을 옮기며 ISO 디렉터리 레코드의 LBA·크기(LE/BE)를 갱신하는 도구를 만들었다(`tools/patch_image_reloc.py`). 재압축한 `MAP.LZB`(156,017 B, 원래 76섹터 예산을 +369 초과)를 LBA 139→166,581로 옮겨 실기 검증했다. | 섹터 수가 늘어나는 파일도 반영할 수 있다. 게임이 `CdSearchFile`로 찾으므로 LBA 변경이 자동 반영된다. 재압축 MAP도 정상 소비된다(RAM `0x80106380`이 `MAP.dec`와 헤더 32 B·스트림 `0x30000`의 64 B까지 바이트 동일). | 꼬리 여유는 150섹터(307 KB)뿐이라 M_BANKS 성장(+약 250 KB)에는 빠듯하다. 트랙 1 확장은 여전히 별도 게이트. | `analysis/emulator/PASS_map_relocated.png`; 세이브스테이트 `state_map_relocated.mcs` | verified-live
2026-08-23 | loader-hook-works | MAP에 트램폴린+13워드 스텁을 넣어 `load_file(11, 0x80168000)`을 추가하는 훅을 만들었고 실기에서 동작을 확인했다. 로더 진입점 exec BP 추적으로 호출 순서(MAP→STAYDAT→D_NAMES)와 인자를 확인했고, 스텁 종료 지점에 `pause_on_hit` BP를 걸어 정지 상태에서 `0x80168000`의 D_NAMES 헤더를 읽었다. | 추가 CD 읽기·목적지 지정·동기 완료가 모두 정상이다. 베이스 표 재지정 기구도 사실상 확인됐다. | 훅 기구 자체는 재사용 가능하다. 목적지와 시점만 바꾸면 된다. | `records/LOADER_HOOK.md` §2 | verified-live
2026-08-23 | emucap-freeze-trap | `tap`/`run_frames`는 frozen 상태를 자동 resume 한다. BP로 멈춘 순간의 메모리를 보려면 `poll_events`와 `read_memory`만 써야 한다. | 이걸 몰라 "적재 실패"로 두 번 오판했다(실제로는 적재 성공 후 힙 클리어에 지워진 상태를 읽고 있었다). | BP 히트 시점 상태가 필요하면 tap/run_frames를 쓰지 않는다. | `records/LOADER_HOOK.md` §2 | verified-live
2026-08-23 | hook-point-too-late | `0x801547CC`는 스테이지 진입용 로더다. 그보다 먼저 뜨는 주인공 설정 화면이 이미 D_NAMES를 읽으므로, 베이스만 새 주소로 돌리면 적재 전에 쓰레기를 읽어 `0x25086FE0` 점프 후 RI 예외로 죽는다. | 훅은 EXE의 오버레이 적재 직후로 옮겨야 한다. | 이분 실험(스텁만 / 스텁+적재 / 스텁+적재+재지정)으로 확정했다. | `records/LOADER_HOOK.md` §3(a) | verified-live
2026-08-23 | scratch-heap-discovered | MAP 초기화가 `*(0x800C0514)=0x80161508`부터 `*(0x800C0518)=0x0004BC28`(310,312 B)를 워드 단위로 전부 0으로 지운다(`0x80108D70` 루프). 시작 주소는 MAP 이미지 끝과 정확히 같고 EXE의 오버레이 적재 목적지와 같은 전역이다. 두 번째 쌍 `0x800C051C/0x800C0520`도 같은 영역을 덮는다. | **오버레이 위 `0x80161508`–`0x801AD12F`는 전부 게임 작업 힙이다.** "144 KiB 빈 대역"이라던 앞선 판정은 스테이지 단계만 본 표본 오류였다. 힙 어디에 두든 MAP 초기화마다 지워진다. | 힙 꼬리를 쓰려면 길이 전역을 줄여야 하고(EXE 비압축이라 워드 하나), 그 전에 게임이 힙 상단을 실제로 쓰는지 증명해야 한다. 가장 큰 압축 오버레이가 195,867 B이므로 앞쪽 196 KB는 필수다. | `records/LOADER_HOOK.md` §3(b) | verified-live_correction
2026-08-23 | dnames-ledger-built | D_NAMES 번역 원장을 span 교체 모델로 만들었다(`translation/dnames_ledger.json`). 레코드 2,653 / span 2,711이고 레코드 44,620 B 중 번역 대상은 21,595 B(48.4%)뿐이다. 보호 필드(raw_hex·span 좌표·원문)와 수정 가능 필드(ko·status·note)를 분리했고, 재실행 시 역문을 보존하고 보호 필드만 갱신하며 달라지면 stale로 차단한다. | 번역 작업의 SSOT가 생겼다. 배치 도구가 보호 스냅샷을 대조해 Agent 응답이 보호 영역을 덮지 못하게 한다. | 검증 게이트 `tools/verify_dnames_ledger.py`(출처·보호 필드·span 무결성·글리프 커버리지·재인코딩 시뮬레이션)를 배치 병합마다 돌린다. | `records/TRANSLATION_LEDGER.md` | passing
2026-08-23 | halfwidth-space-found | 역문에서 유일한 미표현 문자가 공백이었다. low bank 비트맵을 훑으니 **글리프 `0x000`이 전부 0인 빈 8×16**으로, 도달 가능한 유일한 반각 공백이다(`0x0F0`–`0x0FF`도 비었지만 F0–FF는 lead/제어라 만들 수 없다). | 공백을 1바이트로 인코딩할 수 있어 성장률이 1.416배 → **1.332배**로 떨어졌다. | 타깃 charmap 확정 사항으로 `measure_growth.build_lowbank_chars()`에 기록했다. 전각 공백은 `0x3FF`(2바이트)로 구분해 쓴다. | `records/TRANSLATION_LEDGER.md` §4 | verified-static
2026-08-23 | span-classifier-fixes | 두 가지를 고쳤다. (1) `dnames_sections`의 raw는 항상 종단 FF를 뺀 형태인데 마지막 바이트가 확장 글리프 trail로 `0xFF`일 수 있어 `endswith(FF)` 판정이 7개 레코드를 파싱 실패시켰다 — 항상 종단을 붙이는 것으로 수정. (2) 이름 입력 팔레트 행을 '문자 중복률'로 판정하니 `行動終了していないユニットが` 같은 정상 문장까지 걸렀다 — **글리프 ID의 연속성**(인접 차 1–4가 70% 이상)으로 교체. | 파싱 실패 0건이 됐고 실제 메시지가 번역 대상에 남는다. | `bytes.find(FF)` 함정과 같은 종류이니 D_NAMES 밖의 파서에도 같은 기준을 적용한다. | `records/TRANSLATION_LEDGER.md` §2 | fixed
2026-08-23 | batch-b01-names | 기체·파일럿 이름 413개를 번역해 병합했다(status `needs_review`). 번역 완료 span은 1,369개(도너 seed 956 + b01 413), 남은 것은 1,342개다. | 실측 성장률 8,608 B → 11,549 B = **1.342배**. 남은 분량에 같은 비율을 적용한 투영은 **60,250 B**로, 창 한계 65,536보다 5,286 B 작고 현재 슬롯 53,248보다 7,002 B 크다. 역문 고유 한글 음절은 469자. | **뱅킹은 여전히 불필요**해 보인다. 남은 문제는 슬롯 대비 약 7 KB 초과 하나다. 고유명사 표기는 승인 용어 SSOT로 한 번에 고정해야 하고, `竜畸一矢`처럼 출처가 불확실한 이름은 사람 확인이 필요하다. | `translation/batches/b01_names.tsv` | needs_review

2026-08-23 | 측정 | b01~b05 전 배치 병합 완료. 원장 2,711 span 중 2,351개 역문 확정,
360개는 의도적 공란(장식 글리프 145, 이름입력 가나 팔레트 37행, 가라오케 가사, 도안 파편).
| 영향 | **D_NAMES 재삽입 예상 크기 50,738B — 원본 슬롯 53,248B 안에 들어간다(-2,510B).**
실측 성장비 1.330×(번역 구간 18,526B→24,644B). 이전 60,250B 추정은 미번역분에 비율을 곱한
상한이었고, 실제 역문이 채워지자 그 아래로 내려왔다.
| 판정 | **슬롯 초과 블로커 해소.** 로더 훅(자유 RAM 별도 적재)·스크래치 힙 축소·본문 압축은
D_NAMES에 한해 **불필요**. BATTLE 접근자 뱅킹 패치는 이미 적용됐고 동작 중립이라 그대로 둔다
(64KiB 창 상한까지 14,798B 여유 확보 = 향후 문구 확장 안전판).
| 근거 | measure_growth 재실행, verify_dnames_ledger.py PASS.
| 상태 | 확정. 남은 일: 용어 통일(승인 용어 SSOT) → 글리프 할당표(비-lowbank 671종) → 재삽입 빌드.

2026-08-23 | 용어통일 | 도너의 **승인 용어집**을 SSOT로 채택해 원장에 적용했다
(`glossary_names_ko.json` variants 22, `glossary_candidates.json` ko_approved 286,
`msgpool_srw_gloss.json` 120, `despace_nospace.json` 489). 도구 `tools/unify_terms.py`.
| 영향 | 388건 변경(무장 공백제거 228, despace 사전 137, 이형표기 2, 오독 교정 21).
재삽입 예상 50,738B → **50,241B**(슬롯 대비 -3,007B), 성장비 1.330× → 1.303×.
| 판정 | 확정. 다만 `射程`은 승인표의 `사정` 대신 **`사거리`**를 유지했다 — 승인표 자체의
판단 원칙 4번("한자음을 그대로 복사한 부자연스러운 말은 승인하지 않는다")에 `사정`이 어긋나고
폭에도 여유가 있다. `リョウ`는 화자태그(`료`)가 아니라 로스터라 `료마`를 유지했다(도너 notes 근거).
| 근거 | verify_dnames_ledger.py PASS, `translation/dnames_ledger.pre-unify.json`에 이전본 보존.
| 상태 | verified-static

2026-08-23 | 함정 | 승인 용어표는 장음을 `ー`로 쓰는데 이 게임 원문은 `-`(0x2D 계열 글리프)를 쓴다.
정규화 없이 대조하면 장음이 든 용어가 **통째로 누락**된다. 실제로 `サイバスタ-`가 빠져
금지 변형인 `사이버스터`가 원장에 남아 있었다. | 영향 | 도너 대조는 반드시 `ー`→`-` 정규화 후 한다.
| 근거 | 정규화 전 0건 → 후 4건 검출. | 상태 | verified-static

2026-08-23 | 함정 | 무장명 despace를 배치 tsv(b02)로 고르면 한자 무장명만 잡히고
가타카나 무장명을 통째로 놓친다(0건 검출). 무장 표는 **D_NAMES 섹션 S08**이며 S09·S10은
같은 문자열 풀을 가리키는 별칭이라 포인터가 S08로 합쳐진다. 섹션 기준으로 바꾸니 228건.
| 영향 | 표 단위 규칙은 배치 파일이 아니라 섹션 맵(`translation/_section_map.json`)으로 건다.
| 근거 | 도너도 같은 사고를 겪었다(제보 #15a, third-ui/inject_third_ui.py:358). | 상태 | verified-static

2026-08-23 | 폭감사 | 가나는 반각(1칸), 한글은 전각(2칸)이라 이름이 구조적으로 넓어진다.
"섹션 원문 최대 폭 = 상자 수용량"으로 보면 89건이 초과다. | 영향 | **이 대리지표는 과도한 제약이다.**
도너가 같은 계열 표에 실제 출고한 한글을 재니 원문 최대를 넘는 행이 정상적으로 존재한다
(unit_names 원문최대 14 / 한글최대 21, 20행 초과 · full_pilot_names 15/18, 20행 · short_pilot 10/17, 11행).
| 판정 | 이름표 초과분(S04 최대 19, S07 최대 13)은 도너 선례 안이라 **건드리지 않는다**.
설명문 8건만 압축했다(최대 70칸 → 51칸). 나머지 폭 판정은 **런타임 확인 대상**으로 남긴다 —
상자 실폭을 모르는 채 대량 축약하면 패치 품질만 깎는다. | 상태 | needs-runtime

2026-08-23 | 글리프할당 | 16x16 슬롯 1,536칸 중 **M_BANKS 대사가 1,431칸을 쓴다**(구조대로 걸어
11,771/14,336 슬롯 파싱). 가나는 low bank라 16x16은 사실상 전부 한자다. | 영향 | 지금 완전히 자유로운
칸은 94개뿐이라, D_NAMES만 번역한 중간 빌드는 대사 글리프를 덮을 수밖에 없다. 한글 슬롯은
**대사를 번역해야 생긴다** — 폰트 예산과 대사 번역이 결합돼 있다. | 판정 | 할당표를 최종 상태 기준으로
확정하고(`translation/glyph_alloc.json`, 한글 668자 -> ID 0x115–0x6FB, 여유 588칸), 슬롯 순위를
"M_BANKS 미사용 -> 저빈도 -> ID순"으로 정해 중간 빌드 피해를 대사 글리프 사용의 3.5%(2,297/65,110회)로
묶었다. 한글 ID는 이후 단계에서도 재배치하지 않는다. | 근거 | `tools/glyph_census.py`,
`tools/build_glyph_alloc.py`; 자체 정합성 검증 PASS. | 상태 | verified-static

2026-08-23 | 함정 | 글리프 사용 조사를 원시 토큰 스캔(F0–F5+trail)으로 전 파일에 돌리면
C_DEMOG·C_ROBOT·C_PFACE 같은 **그래픽 blob에서 포화**된다(각 1,400칸 이상 "사용 중"). 실제로
1,536칸 전부가 살아 있는 것으로 나와 가용 0이라는 오판을 만들었다. | 영향 | 글리프 census는
구조를 아는 텍스트 blob(D_NAMES 섹션 워크, M_BANKS 헤더->포인터표->0x400 창)만 대상으로 한다.
| 상태 | verified-static

2026-08-23 | 정정 | "재삽입 예상 50,241 B / 슬롯 대비 -3,007"은 **오류**였다. 50,241은 레코드
바이트 합이고 파일에는 헤더 72 + 포인터표 6,690 B가 더 붙는다. 단순 재인코딩은 57,003 B로
슬롯을 3,755 B 초과했다. | 영향 | 크기 판정은 반드시 **완성 파일**로 한다. | 상태 | 정정 완료

2026-08-23 | 빌드 | D_NAMES 재삽입 + 글꼴 주입 + 이미지 반영 완료. 최종 53,164 B / 슬롯 53,248 B
(여유 84 B). 공유 449회로 3,216 B 절약, 설명문 65개 압축으로 618 B 절약. | 판정 | 가장 큰 효과는
**섹션 안에서 긴 레코드부터 배치**한 것(-618 B). 테이블 순서 배치는 짧은 레코드가 먼저 자리를 잡아
접미사 공유 기회를 잃는다. | 근거 | `records/BUILD.md`, verify_reinsert PASS(엔트리 4,090),
이미지 재추출 sha 일치, 디코딩 확인. | 상태 | verified-static, 런타임 미확인

2026-08-23 | 함정 | 접미사 공유에서 "바이트열이 풀 안에 나타난다"는 조건만 보면 안 된다. 토큰이
가변 길이라 **토큰 경계가 아닌 위치에서 시작하면 파싱이 어긋난다.** 검증 없이 재면 46,809 B라는
낙관치가 나오는데 실제 유효 공유는 그보다 훨씬 적다. 후보 위치를 '이미 배치된 레코드의 토큰
시작점'으로 제한해야 한다(`tools/pack_dnames.py`). | 상태 | verified-static

2026-08-23 | 함정 | 글리프 할당표는 역문에서 **파생**된다. 본문을 한 글자라도 고치면 새 음절이
생겨 인코딩이 KeyError로 죽는다(압축 중 `획` 발생). 빌드 순서는 반드시
역문 확정 -> build_glyph_alloc -> reinsert_build 다. 또 타깃 문자표에 없는 문자를 역문에 쓰면
검증기가 unmappable로 잡는다(가운뎃점 `·` -> 원문이 쓰는 `.`으로 교체). | 상태 | verified-static

2026-08-23 | 런타임 | DuckStation으로 패치본 1차 확인: **부팅·한글 렌더 정상.** 주인공 설정 화면
라벨/대사, 배틀 데모 파일럿명 모두 정상 출력, 관측 범위에서 폭 초과 없음. | 상태 | verified-runtime(부분)

2026-08-23 | 함정 | 16x16 비트맵 한글은 PS1 해상도에서 자형이 압축돼 **저해상도 스크린샷 육안
판독이 신뢰할 수 없다.** `이름`을 `0틑`로, `까`를 `刀`로 오독해 "글리프가 깨졌다"고 잘못 진단하고
폰트·인코딩·뱅크 주소를 전부 재검증하는 데 시간을 썼다. 또 창 경계 밖 글자를 오버플로로 오인했는데
뒤에 깔린 창이었다. | 영향 | 렌더 판정은 **같은 글꼴로 기대 문자열을 렌더해 화면과 대조**한다.
| 상태 | verified-static

2026-08-23 | 정정 | "중간 빌드에서 대사 글리프 사용의 3.5%가 영향받는다"는 지표가 잘못됐다.
플레이어가 보는 단위는 대사 한 줄이다. 레코드 기준으로 재니 **11,771개 중 1,427개(12.1%)** 다.
| 원인 | 할당 순위를 "M_BANKS 저빈도 글리프 우선"으로 정했는데 **저빈도 한자는 고유명사에 몰려
있고 화면에서 가장 눈에 띄는 자리다.** | 판정 | 이 빌드는 검증용이며 배포본이 아니다.
대사 번역이 배포의 전제다. | 상태 | verified-static

2026-08-23 | 버그 | **`span_classify.spans()`의 `armed` 게이트가 진짜 라벨을 조용히 버리고 있었다.**
위치 지정 opcode 뒤에서만 글자를 모으고 다른 제어가 나오면 게이트가 꺼져 그 뒤 글리프를
스팬으로 만들지도 않았다. 그래서 `資金`·`EXP順`·`射程が1増えます`·`よろしいですか?` 등이
**원장에 존재조차 하지 않아** 원문 그대로 화면에 남았다. | 발견 | 사용자가 인터미션·시스템설정·
유닛 스테이터스 화면을 실기에서 찍어 보내 일본어가 남은 것을 지적. 정적 검증만으로는 못 잡았다 —
원장 안에서는 모든 span이 번역돼 있었기 때문이다. | 조치 | 게이트를 제거하고 걸러내는 일은
is_drawing/is_kana_grid/is_decoration/is_palette_row에만 맡겼다. 런 경계는 제어 토큰이 정하므로
기존 스팬의 (start,end)는 그대로였고 역문 2,351개가 전부 보존됐다. span 2,711 -> 3,045.
| 근거 | 번역 span 밖 일본어 5,307자 감사 -> 게이트 제거 후 369개 스팬 회수, 그중 실제 라벨 35개 번역.
| 상태 | verified-static

2026-08-23 | 교훈 | 정적 검증(원장 PASS, 재삽입 PASS)은 **번역 대상으로 인식된 것**만 본다.
"인식조차 안 된 텍스트"는 구조 검증을 100% 통과해도 화면에 원문으로 남는다. 커버리지 지표는
`번역된 span / 전체 span`이 아니라 **`번역 span 밖에 남은 원문 글자수`** 로 재야 한다.
| 상태 | verified-static

2026-08-23 | 버그 | 게이트 제거 뒤에도 **휴리스틱 필터가 진짜 라벨을 버렸다.** `is_kana_grid`가
가나로만 된 시스템 메시지 10건을 이름입력 격자로 오인했고(`フォ-マットされていないカ-ドです`,
`コントロ-ラ-をさしこんで…` 등), `is_drawing`은 `EXP順`을 도안으로 판정했다(같은 표의 `HP順`은
통과 — 라틴 2자/3자 차이). | 조치 | 휴리스틱을 더 손대지 않고 **검토된 명시적 허용목록**
`translation/span_overrides.json`을 도입했다. 빌더와 검증기가 같은 규약으로 읽는다.
필터가 버린 스팬 중 일본어가 든 1,315개를 전수 검토해 13개를 확정 포함했고 나머지는
정당한 도안·격자였다. | 판정 | 휴리스틱은 유지하되 **버린 것을 목록으로 남겨 검토**하는 구조가
정답이다. 조용한 손실이 감사 가능한 판단이 된다. | 상태 | verified-static

2026-08-23 | 함정 | 렌더 오독을 **네 번** 했다(`이름`→`0틑`, `까`→`刀`, 창 뒤 배경을 오버플로로,
`4月14日`→`4들14ㅂ`). 마지막 건은 멀쩡한 `月`·`日` 한자를 한글 충돌로 오진해 할당표까지 재검증했다.
| 조치 | `tools/render_expected.py`를 만들었다 — 패치된 STAYDAT의 실제 글꼴로 임의 문자열을 그린다.
**렌더 판정은 반드시 이 도구로 기대 문자열을 그려 화면과 대조한다.** | 상태 | verified-static

2026-08-23 | 버그(중대) | **번역 스팬 17개가 제어 토큰을 삼키고 있었다.** `translatable()`은
위치 opcode(FC/FD)가 없는 레코드를 `[0, 글리프바이트합)` 한 스팬으로 만드는데, 그 오프셋은
**제어 바이트를 빼고 센 값**이라 raw 오프셋과 어긋난다. 그래서 F6/F8/FB가 스팬 안에 들어갔고
재인코딩이 그 자리를 한글로 덮으면서 **제어 토큰이 레코드에서 삭제됐다**.
잃은 것: `HP`[F8]`が,`[F8]`になります`의 **수치 삽입점 F8 2개**, `ビ-ム`의 **동적 selector FB**,
정신 커맨드 설명의 줄바꿈 F6. | 발견 | 사용자가 "파일럿 능력창을 누르면 게임이 멈춤"이라고 제보.
정적 검증(원장 PASS·재삽입 PASS)은 전부 통과했었다 — 스팬 경계가 토큰 경계와 맞는지 검사한 적이
없었기 때문이다. | 조치 | 통짜 스팬은 **제어 토큰이 하나도 없는 레코드에서만** 만들도록 고쳤고,
17개 레코드를 토큰 경계에 맞춰 분리해 35개 스팬으로 재번역했다(제어 토큰 보존).
경계 검사를 상시 검증에 넣었다(현재 0건). | 상태 | verified-static, 실기 재확인 대기

2026-08-23 | 발견 | 도너 `third_align_overrides.py`가 이 엔진의 렌더 규율을 문서화하고 있다:
**"advance exceeds (or phase-mismatches) the retail run before a phase-sensitive anchor (FC/F8...)"**,
`low glyph = 1, high glyph = 1+phase(토글)`. 렌더러가 **반칸 단위**로 진행하고 앵커가 위상에 민감하다.
| 영향 | 한글은 전부 전각, 일본어는 반각 가나를 섞어 써서 같은 폭에 안 들어가는 라벨이 있다.
도너가 `精神ポイント`→`SP`, `身代わり`→`대역`처럼 공격적으로 줄인 이유다. 내 `射程`→`사거리` 대신
도너 승인 표기 `사정`이 옳았다(폭 때문). | 조치 | `tools/align_fit.py`(위상 교정) +
`tools/audit_anchor_fit.py`(감사) + `translation/align_overrides.json`(검토된 짧은 후보) 도입.
FC/F8은 **절대 좌표를 인수로 싣기 때문에** advance 초과가 뒤 요소를 밀지는 않는다 — 앵커에 정말
민감한 건 위상뿐이라, 폭 패딩(+77B로 슬롯 초과)을 빼고 **위상만** 교정한다. | 상태 | verified-static

2026-08-23 | 판단 | 폭 초과 86건(레코드 51개)은 **문구를 훼손하지 않고 경고로 남겼다**
(`translation/_wide_runs.json`). 근거: 확인된 고장은 프리즈 하나뿐이고 그 원인은 폭이 아니라
삼켜진 제어 토큰이며, 폭이 초과된 라벨 다수가 실기에서 정상 출력됐다. 맹목적 절단은
`새겨지려하고있`·`사거`·`데이` 같은 부서진 역문을 만든다. **추측으로 훼손하지 않고 실기로 판정한다.**
| 상태 | needs-runtime

2026-08-23 | 회귀(내 잘못) | **도너의 렌더 모델을 검증 없이 이식해 하드 행을 만들었다.**
도너 `third_align_overrides.py`의 advance/phase 모델(`low=1, high=1+phase 토글`)을 그대로 가져와
앵커 앞 런의 위상을 맞추려고 투명 전각 글리프 `0x3FF`를 삽입했다. 그 결과 원본에 43개뿐이던
`0x3FF`가 137개가 됐고(S00 34 · S01 43 · S02 41 · S13 18), 1차 빌드에서 멀쩡히 통과하던
**시나리오 시작이 0 FPS 하드 행**으로 죽었다(사용자 제보: 캐릭터 설정 후 대화창 안 뜸).
| 근거 | 1차 빌드(패딩·토큰수정 없음)는 인터미션·맵까지 도달했다. 4차에서 토큰 수정과 패딩을
동시에 넣었는데, 토큰 수정은 원본에 **가깝게** 되돌리는 변경이고 패딩은 원본에 **없던 것을 넣는**
변경이다. | 조치 | 위상 패딩을 껐다. `0x3FF` 137 -> 22개, 파일 53,233 -> 53,003 B(여유 245 B).
| 교훈 | **도너에서 가져온 것은 포맷·문자표까지다. 렌더러 동작 모델은 타깃에서 증명하기 전엔
쓰지 않는다.** 도너는 같은 계열이지 같은 엔진이 아니다. 폭 초과는 계속 경고로만 집계한다.
| 상태 | 실기 재확인 대기

2026-08-23 | 원칙 | 회귀를 좇을 때는 **원본에 없던 것을 넣는 변경을 먼저 의심한다.**
토큰 수정(원본 복원)과 패딩(신규 삽입)을 한 빌드에 같이 넣은 탓에 원인 분리에 시간이 들었다.
앞으로 성격이 다른 변경은 빌드를 나눈다.

2026-08-23 | 확정 | 7차 빌드(위상 패딩 제거만 반영)에서 **시나리오 시작 하드 행이 해소**됐다
(사용자 확인: 스테이지 진입 성공). 위상 패딩이 원인이었음이 확정. 한 빌드에 한 가지 변경만
넣어 원인을 분리한 것이 유효했다. | 상태 | verified-runtime

2026-08-23 | 조사 | 화면에 남은 일본어의 출처를 **정적 바이트 탐색으로 확정**했다(에뮬레이터 불필요).
문자표로 후보 문자열을 인코딩해 추출물 전체를 검색:
  유닛 부제 `パ-ソナルトル-パ-` -> **M_BANKS.BIN @0x5CA5C**
  작전 목적 `敵を全滅させる事`   -> **M_BANKS.BIN @0x60234, 0x60376**
  무장명   `プラズマカッタ-`     -> D_NAMES(이미 번역됨)
| 판정 | **D_NAMES 작업은 사실상 완료.** 남은 가시 일본어는 전부 M_BANKS다.
| 교훈 | 런타임 추적을 꺼내기 전에 **인코딩된 바이트 패턴 검색을 먼저** 한다. 문자표가 확정돼
있으면 대부분의 "이 문자열 어디서 오나" 질문은 정적으로 답이 난다. | 상태 | verified-static

2026-08-23 | 도구 | PCSX-Redux 설치: `D:\pcsx-redux` (GPL-2.0, distrib.app dev-win-x64 빌드).
패치본 부팅 확인. emucap에는 **Redux 어댑터가 없다** — CHANGELOG에 "Removed the unused,
unregistered PCSX-Redux runtime draft... PlayStation support continues through the Mednafen adapter"로
남아 있어 MCP 연동 없이 단독 사용한다(GUI 디버거·Lua·GDB 스텁).
| 함정 | 콘텐츠 경로에 **공백/괄호가 있으면 `-iso` 인자가 깨져** OpenBIOS 큐브만 돈다.
공백 없는 cue를 따로 뒀다: `D:\srw4s_ko\game.cue`. 실행:
`pcsx-redux.exe -iso D:\srw4s_ko\game.cue -run`
| 용도 | 다음 단계인 **M_BANKS 라이브 슬롯 모집단 확정**(14,336 슬롯 중 실제 도달분)에 쓴다.
| 상태 | 설치·부팅 확인

2026-08-23 | mbanks-rebank-inflation | 재뱅킹 산출물이 **번역 0개로도** 493,209 -> 647,165 B로 부풀었다. 원인 두 가지: (1) 헤더 244 B 뒤에서 창0을 시작하지 않고 64KiB 경계까지 밀어내 창 하나를 통째로 버렸다, (2) 같은 창 안에서 중복 제거와 접미 공유를 하지 않아 원본이 갖고 있던 레코드 중첩을 전부 잃었다. | 부풀림을 번역량 탓으로 오인하면 "디스크에 자리가 없다"는 잘못된 결론으로 간다. | 항등 모드(`--identity`)로 **패킹 손실과 번역 증가분을 먼저 분리**한다. 창0은 헤더와 같은 64KiB를 쓰고, 창마다 토큰 검증 풀(D_NAMES와 동일 기법)을 돌린다. 결과 547,665 B(268섹터). | `tools/mb_rebank.py`; 항등 647,165 -> 527,578 B | closed
2026-08-23 | mbanks-rebank-deadslot | 표 슬롯 중 5개가 **원본 파일 끝을 넘는 대상**(예: 0x79b1c > EOF 0x786D9)을 가리킨다. 원본에선 죽은 슬롯이지만, 슬라이스가 빈 바이트열이 되어 새 파일에서 EOF를 가리켰다. | "범위 밖" 실패로 나타나 멀쩡한 빌드를 버릴 뻔했다. | 빈 바이트열이면 종단 `FF` 하나를 준다(원본보다 안전). `rel==0` 슬롯도 원본이 실제 대상으로 읽으므로 건너뛰지 않는다. | `tools/mb_rebank.py`; `verify_mb_rebank.py` 범위 밖 5 -> 0 | closed
2026-08-23 | disc-file-lookup | 게임은 실행 파일의 **경로 포인터 표 `0x800E0410`(37엔트리)** 를 거쳐 ISO 디렉터리로 파일을 찾는다. 하드코딩 LBA 표가 아니다. | 파일 재배치가 안전하다 — 디렉터리 레코드만 갱신하면 된다. | M_BANKS 확장분 27섹터를 위해 뒤이은 `M_FIELD`(23섹터)와 `SC_00`(16섹터)을 트랙1 꼬리로 옮기고 M_BANKS는 제자리에서 키웠다. | `records/ARCHITECTURE.md` 로더 규약; `tools/patch_image_reloc.py --grow` | closed
2026-08-23 | disc-tail-budget | 트랙1은 166,731섹터이고 파일 영역은 LBA 166,581에서 끝난다 — **꼬리 여유는 150섹터뿐**이다. 이번에 39섹터를 썼고 111섹터가 남았다. | M_BANKS 번역이 늘면 다시 커진다. 남은 예산을 넘으면 더 큰 이웃을 옮겨야 한다. | 재배치 전 항상 남은 꼬리 섹터를 먼저 센다. | `tools/patch_image_reloc.py` 출력 "여유 잔여 111섹터" | open

2026-08-23 | dnames-filler-drop | 정신 메뉴 프리징을 실기에서 잡았다. PC 가 panic 트랩 `0x800C3A00`(`08030E80` = j self)에 고정, VM 스트림 포인터 `0x801A6B74` = `0x8006A6D2` = D_NAMES `+0x0ED2`, `s5=0xFD`. 그 자리는 **어떤 레코드에도 속하지 않는다.** 원본 D_NAMES 의 S00/S01/S02 풀에는 포인터표가 가리키지 않는 스크립트 조각이 **1,201 B** 들어 있다(예: `0x0eb7~0x0eef` 56 B `ff fd ff ff 11 f2 08 19 ... fe ff fe 00 ff`). 재삽입기는 포인터로 도달 가능한 레코드만 모아 풀을 다시 쌓으므로 이 조각을 전부 버렸고, VM 이 레코드 종단을 넘어 이어 읽다가 어긋나 panic 했다. | **순서 보존만으로는 부족했다** — 사이에 낀 내용도 지켜야 한다. 앞선 `ORDERED_SECTIONS` 수정은 절반짜리였다. | 순서보존 섹션은 원본 풀을 **바이트 단위로 순회**한다: 레코드 시작이면 재인코딩본을, 아니면 원본 바이트를 그대로 옮긴다. 미참조 조각은 공유 색인에 넣지 않는다. 검사: 전 섹션 원문으로 빌드하면 S00/S01 풀이 원본과 바이트 동일, S02 도 자기 길이만큼 동일. | `records/freeze.txt`; `tools/reinsert_build.py` 풀 순회기 | closed
2026-08-23 | dnames-slot-deficit | 조각 1,201 B 를 되살리면 파일이 54,447 B 로 슬롯 53,248 을 **1,199 B** 넘는다. 레코드 증가분은 +5,773 B, 공유 절약은 2,878 B 라 여유가 없다. | 번역을 줄이지 않으면 안 들어간다. 전각공백 정리(18 B)·`align()` 패딩(이미 0) 같은 잔 지렛대는 없다. | 진단 단계에서는 `SRW4S_SKIP_TR="4,8,17"`(기체명·무장명·BGM명만 원문)로 53,201 B 를 만들어 **스크립트 섹션은 한국어 + 조각 복원** 구성을 먼저 검증한다. 확정되면 S00–S02 텍스트를 줄여 영구 확보한다. 주의: 섹션을 원문으로 되돌리면 공유가 줄어 되레 커지는 조합이 있다(`8,6` → 53,752 > `8` → 53,691). | `tools/reinsert_build.py` SRW4S_SKIP_TR | open
2026-08-23 | glyph-alloc-selfharm | 할당기가 live 를 **미번역 원문 기준으로만** 셌다. 역문 자신이 쓰는 비한글 글리프는 보호 대상이 아니어서 전각공백 `0x3FF` 가 '응'에게 넘어갔다 — 역문 안의 전각공백 18곳이 '응'으로 그려진다. | 자기가 만든 텍스트를 자기가 깨뜨리는 유형이라 원문 검증만으로는 절대 안 잡힌다. | 역문의 모든 비한글 문자와 `{G:xxx}` 마커가 지목한 ID 를 후보에서 제외한다. 확인 결과 16x16 에서 보호가 필요한 건 전각공백 1종뿐이고 나머지 4,090회는 전부 저뱅크였다. | `tools/build_glyph_alloc2.py`; "역문보호 1" | closed

2026-08-23 | dnames-filler-confirmed | 조각 복원 빌드로 **정신 메뉴 프리징이 사라진 것을 실기 확인**(사용자: "정신 메뉴 잘 나와"). 진단 빌드는 기체명·무장명·BGM명만 원문으로 두고 스크립트 섹션은 한국어 + 조각 복원 구성이었다. | `dnames-filler-drop` 의 원인 판정이 확정됐다. | 상시 게이트로 `tools/verify_script_stream.py` 를 만들었다 — 역문을 전부 끄고 빌드해 순서보존 섹션 풀이 원본과 바이트 동일한지 본다. 이 검사가 있었으면 처음부터 잡혔다. | 사용자 확인; `verify_script_stream.py` S00/S01/S02 전부 동일 | closed
2026-08-23 | dnames-slot-closed | 조각 1,201 B 를 되살린 뒤의 1,199 B 초과를 **번역을 하나도 포기하지 않고** 메웠다. 효과 순: 전역 배치(섹션을 가리지 않고 긴 것부터, +별칭 0번 레코드는 소유 표 직후 고정) 213 B / 이름 띄어쓰기 제거(S04·S06·S07, 무장 S08 의 기존 방침 확장) 230 B / 산문·시스템 메시지 다듬기 약 530 B. | 섹션을 원문으로 되돌리는 건 **효과가 거의 없다** — S12 를 통째로 되돌려도 11 B 밖에 안 준다(원문 레코드가 공유에서 빠지면서 상쇄). 크기는 다듬기로 줄여야지 되돌리기로는 못 줄인다. | 최종 53,220 B / 슬롯 53,248 (여유 28 B). 폭 초과 경고 196 -> 140건. | `records/BUILD.md` 빌드 10 | closed

2026-08-23 | font-second-copy | 한글이 전부 뒤섞여 나왔다. 원인은 **글꼴이 디스크에 두 벌 있다**는 것 — `C_DEMOG.BIN` 이 STAYDAT 와 **세 뱅크 모두 바이트 동일한** 사본을 갖고 있고 뱅크 순서만 다르다(`[mid 0x41AFC][low 0x49AFC][high 0x4AAFC]` vs STAYDAT `[low 0x36838][mid 0x37838][high 0x3F838]`). STAYDAT 만 주입해서, mid 뱅크(ID 0x100-0x4FF)에 배정된 한글은 C_DEMOG 의 원본 한자로 나오고 high 뱅크(0x500-0x6FF)에 배정된 것만 한글로 나왔다. 화면이 "한글 반 한자 반"으로 보인 이유다. | 정적 검사는 전부 통과했다 — 인코딩과 폰트가 같은 할당표를 쓰는지만 봤을 뿐 **사본이 몇 벌인지 세지 않았기 때문**이다. 자기 파일 안에서만 정합을 확인하면 이 유형은 절대 안 잡힌다. | 주입기를 `tools/font_inject.py` 로 모으고 소비자를 `COPIES` 에 등록한다. `tools/verify_font_copies.py` 가 추출본 전체에서 뱅크 시그니처를 찾아 **등록되지 않은 사본이 있으면 실패**한다. | `verify_font_copies.py`: 2개 파일 발견, 둘 다 739자 불일치 0 | closed
2026-08-23 | font-bitmap-layout | 16x16 글리프는 **8x16 셀 두 개를 나란히** 둔 형식이다(바이트 0-15 = 왼쪽 8열의 16행, 16-31 = 오른쪽 8열). 행마다 2바이트씩 늘어놓은 row-major 가 아니다. | 처음에 row-major 로 그려 "주입본이 한글이 아니다"라고 오판할 뻔했다. `HangulFont` 는 처음부터 올바른 형식으로 내보내고 있었다. | 비트맵을 눈으로 판정하기 전에 **원본 글리프를 여러 해석으로 그려 어느 것이 제대로 된 한자를 만드는지** 먼저 확인한다. | `extract/DAT/STAYDAT.BIN` 0x37838 글리프 0x100 을 4가지 해석으로 렌더 | closed

2026-08-24 | glyph-box-not-cell | 화면에서 한글이 계속 뭉개졌다. 원인은 **저장 칸(16x16)과 실제 글리프 상자(11x12)가 다르다**는 것. 원본 16x16 글리프 1,536개 중 1,440개가 최대 사용 열 10, 34개가 11, 1개가 12다(행은 2~13). 렌더러는 한 글자를 약 12px 전진시키므로, 16px 설계 글꼴을 넣으면 오른쪽 3~4열이 **다음 글자에 겹쳐 그려진다**. | "칸이 16x16 이니 16px 글꼴을 넣으면 된다"는 추론이 틀렸다. 저장 단위와 조판 단위를 같은 것으로 본 것이 함정이다. | 글꼴을 넣기 전에 **원본 글리프의 실제 사용 범위를 전수 조사**한다. Galmuri11 을 12px 로 그려 열 0~10 · 행 3~13 에 맞췄고, 획 굵기도 원본 한자와 같은 1px 이다. `tools/hangul_font.py` 의 `glyph()` 가 상자를 넘으면 빌드를 실패시킨다. | 열 분포 조사; `records/FONT.md` | closed

2026-08-24 | mbanks-freeze-confirmed | M_BANKS 만 원본으로 되돌린 디스크에서 **스테이지 2 진입과 정신 메뉴 모두 프리징 없음**(사용자 실기 확인). 나머지(한국어 D_NAMES, 글꼴 두 사본)는 그대로였다. | `M_BANKS 재뱅킹`이 프리징 원인으로 확정. 원인 후보였던 D_NAMES 텍스트·조각·글꼴은 모두 무죄. | 재뱅킹을 고칠 때까지 배포본은 **M_BANKS 원본**으로 간다. 대사 번역 385레코드는 보류. 다음 단계: 재뱅킹이 무엇을 깨뜨리는지 좁힌다(창 수 8 -> 9 증가, 헤더 오프셋 0x80000 초과 등이 후보). | 사용자 실기 확인 | open
2026-08-24 | glyph-used-by-code | 버튼 설정 화면의 결정/캔슬/스피드업/전체맵 값이 `매/맥/맨/맵` 으로 나왔다. 이 넷은 **연속 글리프 ID 0x6FC~0x6FF** 이고, 원본은 PS 버튼 기호 `○ × △ □` 다. 이 기호들은 **텍스트 레코드 어디에도 없고 코드가 직접 그린다**. | 할당기는 "텍스트가 쓰는 빈도"로 희생자를 고르므로, 코드가 쓰는 글리프는 빈도 0/저빈도로 잡혀 그대로 덮인다. charmap 도 이 자리를 `飽/割/▲/や` 로 잘못 적어 두어 문자 기준 필터로는 못 거른다. | `build_glyph_alloc2.py` 에 `CODE_GLYPHS` 를 두고 후보에서 제외한다. 앞으로 "화면에 나오는데 원문 텍스트에 없는 기호"가 나오면 여기 추가한다. | 0x6FC~0x6FF 원본 비트맵 렌더 (○×△□) | closed

2026-08-24 | save-holds-glyph-ids | 로드 화면의 주인공 이름이 세이브 화면과 달랐다. 세이브 파일이 이름을 **글리프 ID 로** 저장하기 때문에, 빌드마다 할당표를 다시 만들면 옛 세이브의 이름이 다른 글자로 읽힌다. | 본문을 한 글자만 고쳐도 전체 ID 가 밀려 **이미 만든 세이브가 깨진다**. 배포 후에는 치명적이다. | `build_glyph_alloc2.py` 가 이전 `glyph_alloc.json` 을 읽어 **기존 음절의 ID 를 그대로 유지**하고 새 음절만 남은 자리에 넣는다. 이번 빌드는 739자 전부 유지 / 신규 0. | "이전 배정 유지 739자" | closed

2026-08-24 | space-glyph-decodes-empty | 생일 입력 화면의 숫자가 서로 붙어 나오고 커서가 두 글자를 덮었다. 원문은 `1␣2␣3␣4␣5␣決定` 로 **숫자마다 공백 글리프가 끼어 있었는데**, 공백 글리프 `0x000` 이 charmap 에서 **빈 문자열**이라 추출 단계에서 통째로 사라졌다. 번역자는 처음부터 `12345決定` 로 붙은 문장을 봤다. | 커서는 원문 간격을 기준으로 움직이므로, 공백이 빠지면 커서와 글자 위치가 어긋난다. 전수 조사 결과 **가운데 공백이 빠진 span 21개**가 있었다(끝쪽 공백은 칸 채우기라 무해). | 21개를 모두 원문 간격으로 되살렸다. `tools/verify_inner_space.py` 가 원문 대비 가운데 공백이 적은 span 이 있으면 실패한다. | `verify_inner_space.py`: 0개 | closed

2026-08-24 | mbanks-layout-is-the-cause | 4단 이분 탐색으로 원인을 좁혔다: 파일 재배치·크기 증가·내용 치환은 모두 통과하고, **배치 재구성만** 실패한다. 항등 재뱅킹(번역 0개)이 14,105 슬롯 바이트 일치인데도 대사창에 뜻 없는 글자가 뜨고 멈춘다. | 정적 검증이 통과해도 실기가 깨지므로, 런타임 계약이 (뱅크,슬롯)보다 넓다 — 레코드 **뒤에 오는 바이트**까지 포함한다. D_NAMES 조각과 같은 구조. | 배치를 건드리지 않는 `tools/mb_inplace.py` 경로를 정본으로 삼는다(길이 정확히 보존, 종단 앞을 공백 글리프로 채움). 온전한 번역은 주소 계산 코드 훅이 필요하다. | `records/MBANKS.md` 4단 표 | closed
2026-08-24 | sfc-korean-script | 사용자가 SFC 제4차 슈퍼로봇대전 한글패치(ROM·IPS)를 제공했고 내용 사용을 허락했다. 같은 개발사 이식작이라 스크립트가 같고, **인코딩 방식도 같다**(`F0~F5 + trail` 16x16, `00` 공백, u16 오름차순 포인터표). | 대사 번역을 처음부터 하지 않아도 된다 — 일본어 원문을 열쇠로 SFC 한국어를 끌어올 수 있다. | 다만 PS1 쪽 **공간이 벽**이라, 번역문을 구해도 원문 길이 안에 들어가는 것만 넣을 수 있다. 먼저 SFC 글리프->음절 표를 만들어야 한다. | `reference/sfc-kor/` (ROM은 커밋하지 않는다) | open

2026-08-24 | lzb-encoder-proven | LZB(LZSS) 인코더를 만들어 원본 4개 왕복 검증을 통과했고, `MAP.LZB` 를 **내용 변경 없이 재압축**해 넣은 디스크가 실기에서 정상 동작했다(타이틀·스테이지2·증원). | 프로젝트 시작부터 걸려 있던 **"압축 파일 재삽입 금지"** 가 해제된다. M_BANKS 주소 계산 훅이 가능해졌다. | 포맷·인코더·검증을 `records/LZB.md` 에 정리했다. 압축 파일을 고칠 때는 반드시 `verify_lzb_roundtrip.py` 를 먼저 돌린다. | `records/LZB.md`; 사용자 실기 확인 | closed
2026-08-24 | lzb-flag-interleave | 인코더 첫 시도가 첫 매치에서 바로 깨졌다. 원인은 **플래그 비트와 데이터 바이트가 같은 스트림에 섞여 있다**는 것 — 디코더는 비트 버퍼가 비는 순간 그 자리의 바이트를 플래그로 집어간다. 플래그를 따로 모으는 구조가 아니다. | 비트스트림 포맷을 "플래그 블록 + 데이터"로 넘겨짚으면 반드시 깨진다. | 작성기도 비트가 필요할 때 **현재 출력 위치에 한 바이트를 예약**하고 채운다. | `tools/lzb_encode.py` BitWriter | closed
2026-08-24 | mbanks-odd-pointers | 뱅크 한도를 128KiB 로 늘리려 `rel*2` 훅을 설계했으나, **원본 포인터 대상의 44%가 홀수 오프셋**이라 짝수만 닿는 방식으로는 절반이 주소지정 불가다. 훅과 재뱅킹기를 만들고 나서야 발견했다. | 주소 계산을 바꿀 때는 **원본 포인터가 그 제약을 만족하는지 먼저 세어 본다**. 설계 전에 5분이면 될 확인이었다. | `rel*2` 대신 **마스크 제거**(`base = header[bank]`)로 방향을 바꿨다. 명령 두 줄만 nop 이고 홀짝 제약이 없다. | 포인터 14,285회 중 홀수 6,280 | closed

2026-08-24 | mbanks-header-in-staydat | M_BANKS 재뱅킹 v4 + 훅을 넣었는데 게임이 CD 읽기 대기 루프에서 멈췄고, RAM 헤더가 **원본 값**이었다(디스크 파일은 v4). 원인은 **M_BANKS 의 헤더(244 B)와 포인터표 61개(31,232 B)가 STAYDAT 안에도 통째로 복제**돼 있다는 것 — `+0x72800`(RAM `0x80092800`), `+0x73000`(RAM `0x80093000`). 56개 표 전부 바이트 일치였다. | M_BANKS 파일만 고치면 게임은 **옛 좌표로 읽는다**. 글꼴이 STAYDAT·C_DEMOG 두 벌이던 것과 같은 유형 — **이 게임은 같은 데이터를 여러 곳에 복제해 둔다.** | `build_staydat_ko.py` 가 `SRW4S_MBANKS` 로 M_BANKS 빌드를 받아 STAYDAT 안 사본까지 갱신한다. 진단 단서는 "RAM 버퍼 주소가 STAYDAT 적재 범위(0x80020000~) 안에 있는가" 였다. | 사용자 실기: 헤더 `1:0x1B69` 확인 후 스테이지 2 정상 진입 | closed
2026-08-24 | mbanks-v4-works | **재뱅킹 v4 + 마스크 제거 훅 + STAYDAT 사본 갱신** 조합이 실기에서 통과했다(스테이지 2 진입 정상). | M_BANKS 를 546,726 B(267섹터)로 재배치할 수 있고, 확보 가능한 317섹터 안이라 **약 50섹터(102 KB) 여유**가 남는다. 대사 번역을 넣을 자리다. | v4 = 뱅크마다 `[표 0x200][그 표가 읽는 구간들]` 독립 블록, 보존 범위는 **레코드 종단까지**. 훅 = `lui $v0,0xFFFF`/`and $a2,$a2,$v0` 두 줄을 nop. | `records/MBANKS.md`; `tools/mb_rebank4.py` | closed

2026-08-24 | dnames-section-odd-align | 로드 화면에서 게임이 **CPU 예외 벡터(pc=0x80000080)** 로 튀었다. 크래시 지점 `0x80154828`(섹션·엔트리 -> 텍스트 포인터)에서 `a0-a2 = 0x0343B` = S01 섹션 헤더값이 **홀수**였고, 그 주소에서 `lhu` 를 해 정렬 예외가 났다. **포인터표는 u16 배열이라 짝수 정렬이 필수**인데 재삽입기가 섹션 풀을 빈틈없이 붙이며 그 제약을 지키지 않았다. 원본은 18개 섹션 전부 짝수, 내 빌드는 **11개가 홀수**였다. | 오래 갖고 있던 버그이고 그동안 안 터진 건 우연이다(그 빌드에서 마침 짝수로 떨어졌을 뿐). | 섹션 표 시작을 짝수로 맞추고, 홀수가 하나라도 있으면 **빌드를 실패시키는 게이트**를 `reinsert_build.py` 에 넣었다. | 사용자 실기 덤프; `verify` 게이트 | closed
2026-08-24 | mbanks-translation-live | M_BANKS 대사 번역이 **디스크에 들어가 런타임 경로로 읽히는 것**을 확인했다(로드 화면·스테이지 2·대화 전부 정상). 337개 레코드가 적용됐고 미번역 슬롯은 바이트 단위로 원본과 완전히 일치한다. | 대사 번역 파이프라인이 끝에서 끝까지 열렸다. 남은 것은 **번역문 확보**뿐이다. | 필요한 네 조각: LZSS 인코더 / ASM 훅(마스크 nop) / 재뱅킹 v4(표별 독립 블록, 종단까지 보존) / STAYDAT 안 헤더·표 사본 갱신. 디스크 여유 100섹터(204 KB). | `records/MBANKS.md`; `tools/mb_rebank4.py --tr` | closed
2026-08-24 | glyph-collateral-visible | 미번역 일본어 대사에 한글이 한두 글자씩 섞여 나온다(`토強`, `폴源`). 한글 743자를 넣으려고 사용 빈도가 낮은 일본어 글리프 652칸을 덮었기 때문이다(기록된 4.2%). | 버그가 아니라 **설계된 절충**이다. 대사를 전부 번역하면 덮인 글리프를 쓰는 일본어가 사라져 저절로 해소된다. | 부분 번역 상태로 배포한다면 이 현상을 릴리스 노트에 적는다. | `build_glyph_alloc2.py` 출력 "4.2%" | open
| 2026-08-25 | live-flag-not-reach | 사용자가 "슈퍼로봇계열 첫 시작 스테이지가 일본어"라고 보고했다. 화자 이름은 한글인데 본문만 일본어였다. 원인은 배치 필터 `--live-only` — 원장의 `live` 는 **정적으로 증명된 closure(1,586개)** 일 뿐인데 이걸 "게임에 나오는 것"으로 쓰고 있었다. 실제로는 **9,070개 전부가 표 슬롯이 가리키는 레코드**다(owners 없는 레코드 0개). | closure 를 번역 범위로 삼으면 안 된다. 도달성 판단의 정본은 `owners` 다. | `mb_batch.py export --table H55` 를 추가해 **표(=장면) 단위, 슬롯 순**으로 내보낸다. 빈도순 배치는 표마다 조금씩 갉아먹어 어느 장면도 완결되지 않았다. | 오프닝 = 표 H55 슬롯 0~8 | closed |
| 2026-08-25 | mbanks-alias-drop | `mb_rebank4.load_translations` 가 **레코드 안쪽을 가리키는 포인터(별칭)** 가 있으면 그 번역을 통째로 버리고 있었다. 역문 1,123개 중 **145개(12.9%)** 가 디스크에 못 올라갔다. 원장 기준 진도와 실기 체감이 어긋난 원인이다. | 접미사 공유는 드물지 않다 — 전체 레코드의 8.9%(803개)가 별칭을 가진다. 조용히 건너뛰면 진도 수치가 거짓말을 한다. | 번역문을 싣고, 별칭마다 **원문 접미사 사본**을 뒤에 붙여 그 포인터가 원래 내용을 그대로 읽게 했다. 지금 비용 7,248 B. 새 게이트 `verify_mb_rebank4_tr.py` 가 "원본 그대로 아니면 역문과 정확히 일치" 를 강제한다. | 항등 회귀 PASS(불일치 0) / 번역 빌드 PASS(설명 안 됨 0) | closed |
| 2026-08-26 | textbox-layout-contract | 사용자 실기에서 대사가 창을 넘어 **줄 끝이 같은 줄 맨 앞으로 감겨** 앞부분을 덮어썼다("들의토르「태평양도..."). 조판 규약을 원본 데이터로 확정했다 — **한 줄 320px, 한 페이지 3줄**. 폭은 글리프 뱅크로 정해진다: 저뱅크(1바이트, ID<0xF0)는 **8x16 반각**(stride 16 으로 완벽히 그려짐 — `「`,`,` 로 확인), 중/고뱅크(2바이트)는 16x16 전각. `{B:xxxx}` 이름 삽입은 4전각(64px) 예산. | 원문이 "줄당 26자"까지 들어가는 건 문장부호가 반각이기 때문이지 창이 넓어서가 아니다. 글자 수로 세면 틀린다. | `tools/mb_layout.py` 로 규약을 코드화하고, `verify_mb_ledger.py` 의 `{N}` 개수 일치 규칙(조판이라 의미 없음)을 **줄 폭·페이지 줄 수 게이트**로 교체했다. `tools/reflow_ko.py` 가 역문을 다시 조판한다. | 원본 줄 폭 분포: 이름표식 없는 줄 99.97% <=288px / 있는 줄 문자부분 99.7% <=224px. 관측된 감김 위치 4건이 전부 320px 모델과 일치 | closed |
| 2026-08-26 | reflow-korean-spacing | 역문을 다시 조판하려면 기존 `{N}` 을 지워야 하는데, 한국어는 띄어쓰기가 있어서 그냥 지우면 단어가 붙는다("모르는당신을"). `{N}` 경계 358개를 전수 확인해 대부분이 어절 경계임을 확인하고, 어절 중간 35곳만 예외표로 뺐다(`reflow_ko.py:NO_SPACE`). 앞이 `...` 로 끝나거나 뒤가 `.` 로 시작하면 붙인다. | 일본어 원문에는 없던 문제다 — 조판 도구를 언어 중립적으로 만들면 한국어에서 조용히 깨진다. | 앞으로의 역문은 **`{N}` 없이 한 줄로 쓰고** 조판은 도구에 맡긴다. | `verify_mb_ledger.py` PASS | closed |
| 2026-08-26 | batch-tsv-column-drift | 원장을 배치 TSV 에서 복구할 때 `mb_batches/m01_short.tsv` 만 열이 5개(id/start/end/jp/ko)라 `ko` 자리에 **숫자가 들어갔다**. 표식 검증이 7건을 잡아 발견했다. | 같은 확장자·같은 폴더라고 같은 형식이 아니다. 열 위치로 읽으면 조용히 오염된다. | 로더가 헤더(`# id\tjp\tko`)를 확인하고, **jp 열이 원장과 바이트 일치하는 행만** 정본으로 쓴다(merge 와 같은 규칙). | `verify_mb_ledger.py` marker 0건 | closed |
| 2026-08-26 | linewidth-recalibrated-304 | 320px 로 잡았던 줄 폭이 실기에서 여전히 넘쳤다. 화면에서 눈금을 다시 맞췄다 — **312px 짜리 줄은 8px(반각 1칸), 320px 짜리는 16px(전각 1칸) 넘쳤고 304px 짜리는 딱 들어갔다**. 창 = **304px**. 이름 예산도 4전각 -> **5전각(80px)** 으로 올렸다(기본 애칭이 3자라 여유가 필요하다). | 원본 데이터의 분포로 잡은 한도(288~296px 이하 99.97%)가 맞았고, 내가 여유를 잘못 얹었다. **실기 눈금 없이 상한을 추정하지 말 것.** | `mb_layout.py` 의 `LINE_PX=304`, `NAME_PX=80`. 역문 294개 재조판, 넘친 29페이지는 본문을 줄였다. | 실기 스크린샷 4장의 감김 폭이 전부 이 값과 일치 | closed |
| 2026-08-26 | protagonist-name-6byte-field | 주인공 설정 화면의 기본 이름이 `그레이集ㅋ리진=우리진` 처럼 깨졌다. 이름 설정에 들어갔다 나오면 애칭이 **`그레이`(3자)** 로 나오는 것이 단서였다 — 내 역어는 `그레이스`(4자, 8바이트)다. **이름 칸은 6바이트**다. 원본 이름 선택지도 전부 6바이트 이하(`スタ-ロ-ド`, `イルムガルト` 가 최대 6)라 일치한다. | 한글은 2바이트라 **이름은 3자까지**다. 글자 수가 아니라 바이트로 잘리므로 4자를 넣으면 마지막 글자의 앞바이트만 남아 뒤 데이터와 짝지어져 엉뚱한 글리프가 나온다. | 이름 선택지 8개를 3자로 줄였다(그레이스->그레이, 스타로드->스타로, 카자하라->카자하, 라이클링->라이클, 레난제스->레난제, 패트리샤->패트리, 일무가르트->일무갈). 기체명은 이 제약이 아니므로 손대지 않았다. | `DN:B247`~`DN:B2C0` 구간 6바이트 초과 0건 | closed |
| 2026-08-26 | table-order-is-pool-order | 표(시나리오)의 이야기 순서를 몰라 배치 순서를 계속 헤맸는데, **레코드 오프셋 중앙값으로 표를 정렬하면 이야기 순서가 그대로 나온다**. H02(제1화) -> H03 -> H04 -> H00 -> H05 -> H18 -> H31 -> H06 ... 순이다. 데이터 풀이 시나리오 순으로 깔려 있다. | 시나리오 순서를 SC_xx 해석 없이 공짜로 얻는다. 번역 순서를 플레이 순서와 맞출 수 있다. | 새 표를 고를 때 `중앙 오프셋` 정렬표를 본다. | H02 가 1화인 것이 실기로 확인됐고 정렬 1위 | closed |
| 2026-08-26 | mbanks-growth-vs-disc | M_BANKS 가 241섹터(원본) -> 283섹터(역문 18.5%)로 커져 `SC_01` 과 부딪혔다. `SC_01`~`SC_05`(28섹터)를 트랙1 꼬리로 옮겨 6015까지 확보했다(꼬리 여유 72섹터). | **선형 외삽하면 전량 번역 시 약 468섹터**가 필요한데 확보 가능한 건 308섹터쯤이다. 한글이 항상 2바이트인데 원문 가나는 1바이트라 커진다. 언젠가 막힌다. | 지금은 여유가 있다. 막히면 (a) STAYDAT 뒤 큰 이웃 재배치, (b) 트랙1 확장, (c) M_BANKS 자체 압축 중에 골라야 한다. **재배치 전 항상 꼬리 잔여를 먼저 센다.** | `patch_image_reloc.py` 출력 "여유 잔여 72섹터" | open |
| 2026-08-27 | korean-particle-after-marker | 실기에서 `린면 됩니다`, `린라고 부르지만`, `230가` 처럼 조사가 틀리게 나왔다. `{B:xxxx}` 는 실행 중에 이름이 들어가므로 **받침 유무를 알 수 없다** — 한국어 조사(이/가, 은/는, 라고/이라고)를 고를 수 없다. | 일본어는 조사가 받침에 안 달려서 원문에는 없던 문제다. 마커 뒤에 조사를 그냥 붙이면 반드시 절반은 틀린다. | 받침과 무관한 형태로 쓴다 — ` 이라고`, ` 이면`, `이(가)`, ` 이에요`. 마커+조사 25곳을 전수 치환했고(`tools/ko_replace.py`), 숫자 뒤도 `이(가)` 로 바꿨다. | `MB:0216F` 등 25건 / `DN:55EB` 등 5건 | closed |
| 2026-08-27 | alias-inside-unchanged-prefix | 마징가 컷인이 일본어로 남았다. 원인은 **바이너리 앞머리 + 대사 꼬리** 로 된 레코드(`MB:00014`)를 번역 대상에서 빼 둔 것, 그리고 재뱅커가 별칭에 **원문 접미사 사본**을 주던 것. | 별칭이 항상 원문을 읽어야 하는 건 아니다. **앞부분이 안 바뀌었다면** 그 안의 별칭은 번역문의 같은 상대 위치를 읽으면 된다. | 재뱅커가 원본과 역문의 공통 접두 길이를 재서, 그 안의 별칭은 번역문 안으로 보낸다. 검증기도 같은 규칙으로 기대값을 만든다. 혼합 레코드 4개(`MB:00014`/`004DD`/`05058`/`6BFAF`)는 앞머리를 그대로 두고 꼬리만 번역했고 `reflow_ko.NO_REFLOW` 로 재조판에서 뺐다. | `verify_mb_rebank4_tr.py` 설명 안 됨 0 / 빌드에서 `マジ-ン,ゴ-` 0건 | closed |
| 2026-08-27 | yesno-box-too-narrow | 출격 확인창의 `네`/`아뇨` 가 선택창을 넘쳤다. 원문은 `はい`(반각 2자=16px) / `いいえ`(반각 3자=24px) 인데, **한글은 전각 16px 이라 `아뇨` 가 32px** 로 8px 넓다. 선택창 크기 바이트(`f5 .. .. 05 86`)는 네 확인창(`DN:09FF`/`0C4C`/`1B7C`/`29FB`)이 전부 같아서 창을 넓히는 길은 없었다. | 저뱅크(반각 8px) 원문을 전각 한글로 옮기면 **글자 수가 같아도 폭이 두 배**다. 고정 폭 UI 칸은 픽셀로 다시 재야 한다. | 24px 안에 드는 한 글자로 바꿨다 — `예` / `노`. 8바이트 절약도 됐다(D_NAMES 여유 20 -> 28 B). | `DN:09FF` 등 4개 레코드 8개 span | closed |
| 2026-08-27 | alias-only-block | 스테이지 4 첫 적 대사(`MB:05058`)가 번역했는데도 일본어였다. 그 레코드의 **정본 시작은 표 H03 에만 있고 H04:E0000 은 별칭(0x05189)만 가리킨다**. 재뱅커는 표마다 독립 블록을 만들면서 자기 슬롯이 가리키는 구간만 훑으므로, H04 블록에서는 정본 시작을 만나지 못해 원문을 그대로 복사했다. | 별칭 처리를 "정본 레코드를 만났을 때 곁들이는 것"으로만 짜면, **정본이 없는 블록**에서 조용히 원문이 남는다. 번역률 수치로는 안 보인다. | 앞부분이 안 바뀐 별칭은 `alias_tr[별칭] = (역문[rel:], 원본길이-rel)` 로 따로 등록해, 블록 순회가 별칭에 바로 착지해도 역문을 싣는다. 역문 적용 슬롯 2,243 -> **2,371**. | `H04:E0000` 이 한국어로 나옴 확인 | closed |
| 2026-08-27 | yesno-text-x-follows-width | 확인창 `네/아뇨` 를 폭 문제로 보고 `예/노`(각 16px)로 줄였더니 **글자가 창보다 36px 더 왼쪽으로** 갔다. 실측: 창 x는 `f5 d0`(=208px)이고 글자 x는 폭에 따라 움직인다(아뇨 32px -> 203, 노 16px -> 172). 즉 **좁힐수록 더 어긋난다.** | 폭이 문제라고 단정하고 줄인 것이 오히려 악화시켰다. 고정 폭 UI라도 **글자 위치가 폭에서 파생**될 수 있다. | `네/아뇨` 로 되돌렸다(사용자 지시이기도 하다). 남은 어긋남은 커서 막대 위치이며, 그 좌표는 `fc 02 02` 라 span 밖 바이트여서 지금 구조로는 못 건드린다. | 사용자 실기 확인 대기 | open |
| 2026-08-27 | noreflow-needs-tail-layout | 바이너리 혼합 레코드를 재조판에서 빼 놨더니(`NO_REFLOW`) **꼬리 대사에 줄바꿈이 하나도 없어** 실기에서 창을 넘어 감겼다(`MB:05058`). | "재조판하면 앞머리가 깨진다"는 맞지만, 그렇다고 **조판을 아예 안 하면 안 된다**. 앞머리는 그대로 두고 **꼬리만** 조판해야 한다. | 원문과 역문의 공통 접두를 잘라 내고 꼬리만 `_split_line` 으로 조판해 `{N}` 을 넣었다. 대상 5개 모두 3줄 안에 들어간다. | `MB:05058` 288/296/240px 3줄 | closed |
| 2026-08-27 | dnames-raw-byte-patch | 확인창 글자 좌표 `fc 02 02` 는 텍스트 **구간(span) 밖** 이라 원장으로는 못 건드린다. | D_NAMES 재삽입은 구간만 갈아 끼우고 나머지는 통과시킨다 — 좌표·크기 같은 값은 손이 안 닿는다. | `tools/patch_dnames_raw.py` — 재삽입 뒤 **유일하게 일치하는 바이트 문맥**을 찾아 그 자리만 고친다(0개나 2개 이상이면 실패). 규칙은 `tools/dnames_raw_patch.json`. 첫 적용: 출격 확인창 글자를 +3칸(24px) 오른쪽으로. | 실기 확인 대기 — 어긋남이 남으면 첫 인자만 조정하면 된다 | open |
| 2026-08-27 | linewidth-296-final | 정확히 304px 인 줄에서 마지막 `」` 가 잘렸다. 앞서 "304px 은 딱 들어간다"고 본 근거(`MB:6C265` 3줄)는 사용자가 "잘리는 것 같다"고 한 바로 그 줄이었는데 내가 넘겼다. **실제 한도는 296px.** | 사용자가 애매하게 지적한 것을 "확실한 반증"이 없다고 넘기면 눈금이 8px 어긋난 채로 굳는다. 애매한 신호도 반증이 나올 때까지는 살려 둬야 한다. | `LINE_PX = 296`. 역문 283개 재조판, 4줄이 된 38개는 본문을 줄였다. | `MB:04CF4` 208/88px 2줄 (사용자 지적 그대로 다음 줄로) | closed |
| 2026-08-27 | term-drift-between-ledgers | 대사는 `게터G`, 출격 목록은 `겟타1` 로 표기가 갈렸다. **D_NAMES(기체·인물·무기 이름)와 M_BANKS(대사)를 따로 번역해 왔기 때문**이다. | 원장이 둘이면 같은 고유명사가 조용히 갈린다. 사용자는 두 화면을 나란히 보므로 바로 눈에 띈다. | D_NAMES 의 가타카나 span 역어를 정본으로 삼아 대사와 대조하는 점검을 돌렸다(55건 중 띄어쓰기 차이를 뺀 10건이 진짜 불일치). 통일: 겟타, 드레이도우, 깃자, 마크토민, 첸, 아프로다이A, 카크리콘, 액시즈 / D_NAMES 를 고친 것: 다이모스, 로펫, 번들. | `verify_mb_ledger.py` PASS | closed |
| 2026-08-27 | fc-is-one-operand | 표식 `{C:06F0}ビ進する` 처럼 **말이 안 되는 원문**이 보여 파고드니, FC 제어를 **2인수로 읽고 있었다**. 실제는 1인수다. 근거 셋: (1) 2인수로 읽으면 여는 「(0x3E) 가 인수로 삼켜져 **155개 레코드에만 여는 따옴표가 없다**(나머지 9,062개엔 낱글자로 있다), 1인수로 읽으면 154개가 되살아난다. (2) `fc 05 3e fc 06 f0c2 f1b3 58 8a` 는 1인수로 `「発進する!!」`, 2인수로는 `ビ進する` — 형제 레코드 `MB:0CF07`(`fc 06` 없음)이 같은 문장이다. (3) D_NAMES 원본 문자열 4,090개를 파싱하면 2인수는 22개가 깨지고 1인수는 0개다. | 마크업을 다시 인코딩하면 바이트가 같아서 **출고물은 멀쩡했다** — 그래서 2,552개를 번역하는 동안 안 드러났다. 드러나는 곳은 둘이다: 조판이 삼켜진 「(16px)를 못 세고, 앞으로 `{C:xxF0}` 7건(H30 발진 대사)을 번역하면 **F0 이 내 한글 앞바이트와 짝지어져** 통째로 깨진다. 그 표를 막 번역하려던 참이었다. | `text_codec.parse_record(rec, arity)` 로 아리티를 인수화하고 `MB_ARITY`(FC=1)를 M_BANKS 쪽에만 넘긴다. **D_NAMES 기본값은 2로 남겼다** — 1로 바꾸면 22개 레코드의 경계가 짧아져 그 뒤 번역 스팬 10건(`레벨`, `이(가) 장착 중...`)이 레코드 밖으로 밀려나고, 이미 검증·출고된 파일이라 지금 흔들 이유가 없다. 원장/배치는 `tools/migrate_fc_arity.py` 로 이관(jp 339 · ko 79 · 배치 3,714행). | 이관 직후 재빌드가 **바이트 동일**(M_BANKS `68b5f00b`, D_NAMES `4344de8f`) — 표기만 바뀌고 출력은 그대로임을 증명 | closed |
| 2026-08-27 | prefix-is-data-not-text | 위 이관 뒤 표식 검증이 `MB:3AE49`/`MB:3AEF3` 2건을 잡았다. 앞머리가 `{C:08}` + 16바이트인데 그 16바이트는 글자가 아니라 **증가하는 u16 8개짜리 표**(첫 값 0x0010 = 표 자기 크기)였다. 구 역문은 그 표를 통째로 버렸고, 여는 「(0x3E)마저 `fc 3e` 로 흘려보내 실기에서 「가 아예 안 그려지고 있었다. | 마크업이 데이터를 글자로 그려 주면 **번역해도 되는 것처럼 보인다**. 표식 짝 검사가 유일한 방어선이었다. | 원문 앞머리를 첫 「 까지 그대로 두고 대사만 갈아끼웠다. 폭 계산이 성립하지 않으므로 `reflow_ko.NO_REFLOW` 에 넣었다. **역문이 원문 앞머리를 버리지 않았는지**는 표식 짝 검사가 이미 잡는다(화자 이름 앞머리까지 비교하는 넓은 검사를 시도했다가 2,008건을 일본어로 되돌려 백업에서 복구했다 — 넓은 그물은 정상 번역을 먼저 잡는다). | `verify_mb_ledger.py` PASS / D_NAMES sha 출고본과 동일 | closed |
| 2026-08-27 | menu-page-N-is-structure | 선택지 페이지(` Sガンダム{N}ガンタンク`)의 `{N}` 을 재조판이 지워 **선택지 6건이 한 줄로 붙어** 출고돼 있었다. 선행 공백도 사라져 있었는데, 원인은 재조판 로더의 `q[2].strip()` 이었다 — 배치에서 읽을 때마다 커서 자리를 깎아 냈다. | 대사에서 `{N}` 은 조판이지만 선택지에서는 **구조**다. 하나로 뭉치면 두 번째 선택지가 화면에서 사라지고, 들여쓰기를 잃으면 커서 자리만큼 왼쪽으로 밀린다. `.strip()` 처럼 무해해 보이는 정리가 의미 있는 공백을 먹는다. | `reflow_ko.is_menu()` — 선행 공백 + 따옴표 없음 + `{N}` 으로 나뉜 짧은 토막 4개 이하면 선택지로 보고 그 페이지는 재조판하지 않는다. 판정은 **원문 페이지**로 한다(역문은 이미 망가져 있을 수 있다). 로더의 `.strip()` 제거. `verify_mb_ledger` 에 `menu_page` 항목 추가 — 선택지 페이지는 원문과 같은 들여쓰기·`{N}` 수를 가져야 한다. | 일부러 `{N}` 을 지워 게이트가 발화하는 것까지 확인 | closed |
| 2026-08-27 | fc08-variant-table | `{C:08}` 뒤 16바이트(u16 8개)는 글자가 아니라 **같은 대사의 파일럿별 변형을 가리키는 표**다. 원본 11개 레코드 중 9개에서 첫 항목이 그 레코드의 여는 「 에 정확히 떨어진다(무작위 기준 2%). 첫 변형만 한국어로 갈면 길이가 달라져 나머지 항목이 전부 어긋난다. | 원장이 그 16바이트를 글자로 그려 주기 때문에 **번역해도 되는 것처럼 보인다**. 표식 짝 검사도 통과한다 — 표식이 아니라 글리프이기 때문이다. | `{C:08}` 을 가진 레코드는 번역하지 않는다(8건 되돌림: `MB:004DD` `0CFCC` `0D039` `0F771` `0FB43` `0FBDD` `3AE49` `3AEF3`). 덧붙여 **재뱅킹이 이미 11개 중 6개의 표를 깨뜨린다** — 항목이 레코드 밖을 가리키면 이웃이 옮겨 가면서 어긋난다. 번역과 무관한 기존 결함이다. | 빌드에서 첫 항목이 「 에 떨어지는 표 5/11 (원본 9/11) | open |
| 2026-08-27 | pointer-table-record | `MB:0CC39` 을 번역했더니 재뱅킹 검증이 슬롯 하나를 '설명 안 됨' 으로 잡았다. 그 레코드의 '글자'는 `9ミ9ミ9ミ...` 인데 실제로는 **u16 포인터 표**였고(`39 ce` = 0xCE39, 자기 별칭 목록과 일치), 재뱅커가 주소를 새 위치로 갱신하는 대상이었다. | 역문으로 통째로 갈아끼우면 **옛 주소가 그대로 박힌다**. 눈에 보이는 대사는 끝의 `「発進!!」` 한 줄뿐인데, 그 한 줄 때문에 표 전체를 잃을 뻔했다. | 번역 대상에서 뺐다(`발진!!` 한 줄만 원문으로 남는다). 재뱅킹 검증의 '설명 안 됨' 이 유일한 방어선이었으므로, 그 게이트를 파이프로 가리지 말 것 — `cmd | tail` 은 tail 의 종료코드를 쓰므로 `&&` 사슬이 FAIL 을 지나쳐 버린다. 빌드 사슬은 `set -o pipefail` 로 돌린다. | `verify_mb_rebank4_tr.py` 설명 안 됨 0 | closed |
| 2026-08-29 | fb-arity-depends-on-type | 스테이지 대사가 일본어라는 제보를 쫓다가 `{B:F00C}- あしゅら男爵を倒す事` 처럼 앞머리에 뜻 없는 글자가 붙은 레코드를 만났다. **FB 도 인수 개수가 고정이 아니었다** — 형식 바이트(두 번째 인수)가 `0C`/`08` 이면 4바이트, `80`/`12` 등 이름 삽입은 2바이트다. 판정: 형식 0C/08 레코드 28개에서 화자 이름이 SSOT 와 맞는 것이 2바이트로는 **0개**, 4바이트로는 **12개**(나머지는 화자가 없는 승리 조건이고 `ギャブレ-を倒す事` 처럼 깨끗해진다). `{B:490C}ご{8:9B}ラリア` 가 4바이트로는 `ガラリア` 가 된다. | FC 때와 달리 **출고물이 실제로 깨져 있었다.** 역문이 그 2바이트 자리에 한글을 밀어 넣어 실기에서 첫 글자가 인수로 먹히고 있었다 — `렛시`→`시`, `토드`→`드`, `쇼「...」`→`「...」`. 12건. 표식 개수는 맞으니 짝 검사도 통과했다. | `text_codec.MB_ARITY[0xFB]` 을 **호출 가능 객체**로 바꿔 뒤따르는 바이트를 보고 2/4 를 정한다(`parse_record` 가 callable 아리티를 지원). 이관 뒤 먹힌 글자를 표식 인수에서 되읽어 복원했다. `mb_layout.atom_px` 도 고쳤다 — 인수 4바이트짜리 `{B:}` 는 이름 삽입이 아니라 아무것도 안 그리므로 80px 가 아니라 0px 다. | `verify_mb_ledger` PASS / FB 표식이 원문과 다른 역문 0개 | closed |
| 2026-08-29 | table-order-heuristic-fails | `H52` 는 오프셋 중앙값 정렬로는 뒤쪽인데 실제로는 스테이지 4~5 에서 나온다(도시 맵·다바/암·브로켄). `H01` 은 스테이지 표가 아니라 **승리 조건 풀**이라 매 스테이지 뜬다. | `table-order-is-pool-order` 로 얻은 순서는 **스테이지 표에만** 맞는다. 조언·승리 조건·음성 같은 공유 풀은 오프셋이 뒤쪽이어도 초반부터 나온다. 순서만 믿고 진행하면 사용자가 계속 일본어를 만난다. | 사용자 제보가 나온 표를 우선한다. 초반 표와 레코드를 공유하는 표를 owners 교집합으로 찾는 방법은 **공유가 0** 이라 안 먹힌다 — 풀은 자기 레코드를 따로 갖는다. | H52 196건 · H01 51건 번역 완료 | open |
| 2026-08-29 | victory-box-is-narrower | 승리 조건 창은 대사창(296px)보다 좁다. 원문 실측 최대 **232px · 2줄**(`6タ-ン以内に,誰かが敵基地にたどり着く事`), 엔딩 문구는 **248px · 2줄**. | 296px 로 재조판하면 창을 넘는다. 조판 규약이 창마다 다른데 `LINE_PX` 하나로만 다루고 있었다. | 승리 조건 25건은 전부 **한 줄 232px 이하**로 맞췄다(`~を倒す事`→`~ 격파`, `~が生き延びる事`→`~ 생존` 으로 통일). 엔딩 3건은 손으로 조판하고 `NO_REFLOW` 에 넣었다. | 승리 조건 232px 초과 0건 | closed |
| 2026-08-29 | variant-N-is-structure | `{B:1E80}「...」{N}{B:1E80}「...」` 는 한 레코드에 든 **두 갈래 응답**이다(주인공에 따라 한쪽만 나온다). 재조판이 이 `{N}` 을 지워 3건이 한 줄로 붙어 출고돼 있었다. | 선택지(`menu_page`)와 같은 부류다 — `{N}` 이 조판이 아니라 구조인 자리가 또 있었다. 줄 수도 **갈래마다** 세야 한다(한 번에 한 갈래만 뜨므로 합쳐서 4줄이어도 괜찮다). | `mb_layout.VARIANT` 로 경계를 잡아 재조판은 갈래별로 하고 경계는 보존한다. `violations` 도 갈래별로 줄을 센다. `verify_mb_ledger` 에 `variant_page` 검사 추가. | 일부러 경계를 지워 게이트 발화 확인 / PASS | closed |
| 2026-08-29 | single-dot-break-splits-names | 재조판이 점 **하나** 뒤에서도 줄을 끊는다. 그 자리는 다시 합칠 때 `need_space` 가 공백을 넣으므로 `라.기어스` 가 `라. 기어스` 로, `윌.윕스` 가 `윌 위프스` 로 벌어졌다. | `_breaks` 의 주석은 '연속된 점이 끝난 자리' 라고 적혀 있었지만 코드는 점 하나에도 걸렸다. **주석이 맞고 코드가 틀린** 경우다. | 점이 두 개 이상일 때만 끊도록 고쳤다(`seq[i-2] == "."`). 벌어진 3건을 되돌리고 점 든 이름 6개를 `reflow_ko.NO_SPACE` 에 등록했다. | `라.기어스`·`윌.윕스` 복원 확인 / PASS | closed |
| 2026-08-29 | mbanks-place-not-grow | 55.8% 시점에 M_BANKS 가 **312섹터**가 되어 제자리 확장(`--grow`)으로는 뒤의 `STAYDAT`(LBA 6016)을 넘게 됐다. 앞쪽 `DAT/IP.BIN`(5699..5707, 9섹터)을 꼬리로 옮기고 `--place DAT/M_BANKS.BIN=...@5699` 로 **시작 LBA 를 앞당겼다**. | 성장 한계는 뒤가 아니라 **앞뒤 양쪽**으로 푼다. 뒤의 STAYDAT 는 249섹터라 꼬리 여유(63)로 못 옮기지만, 앞의 작은 파일 하나면 충분했다. | 지금 자리(5699..6015)는 최대 317섹터. 앞 파일을 꼬리 여유 63섹터만큼 더 옮기면 **380섹터**까지 간다. 전량 번역 시 선형 외삽으로 **368섹터**가 필요하니 아슬아슬하게 들어간다. 다음에 막히면 옮길 후보(작은 것부터): `D_UNIT`(3) `D_PILOT`(5) `D_WEAPON`(6) `D_ROBOT`(7) `D_NAMES`(26) `D_PALET`(29) `D_OBJCT`(47). | `--place` 검증에서 'ISO가 LBA 5699 크기 638168로 보고, 내용일치=True' | open |
| 2026-08-30 | mbankb-is-cutin-text | 사용자가 제보한 겟타 변신 컷인 일본어는 M_BANKS 가 아니라 **`BTT/M_BANKB.LZB`**(압축 33,362 B → 해제 55,353 B)에 있다. 겟타·단쿠가·잔봇·라이딘·마징가 무기명·"너무 멀다" 류 전투 대사가 전부 여기 모여 있고 지금까지 손대지 않았다. 구조는 M_BANKS 와 같다(61엔트리 u32 헤더 → 뱅크 표 → 0xFF 종단 레코드). 쓰이는 뱅크는 20–27, 58–60 의 11개. | M_BANKS 를 100% 번역해도 전투 컷인은 일본어로 남는다. 별도 파이프라인이 필요하다. | `tools/build_mbankb_ledger.py` 신규. LZB 왕복은 이미 실기 증명됨(`lzb-encoder-runtime-proven`)이라 재압축 경로는 열려 있다. | 왕복 검증 PASS / 대사 레코드 170건 3,687 B 식별 | open |
| 2026-08-30 | bank-table-length-varies | M_BANKB 의 뱅크 표는 **256칸 고정이 아니다**(80·208·240·250·255칸으로 제각각). M_BANKS 코드를 그대로 가져와 256칸으로 읽었더니 레코드 바이트를 포인터로 오해해 가짜 레코드가 쏟아졌다(312건 중 진짜 대사는 170건, 나머지는 표거나 문장 중간에서 시작하는 조각). | 조각 레코드(`-ジがでかすぎるか!?」`)를 번역했다면 앞 레코드의 꼬리를 덮어 그 대사를 통째로 깨뜨렸을 것이다. | 표 길이는 **어떤 슬롯도 표 자신을 가리키지 않는 최대 길이**로 구한다(`nslots()`). 첫 슬롯 값만 믿으면 안 된다 — 뱅크끼리 레코드를 공유해 첫 슬롯이 최소가 아닐 수 있다. | 표 길이 확정 후 레코드 312 → 232, 그중 대사 170 | closed |
| 2026-08-30 | mbankb-has-no-slack | M_BANKB 해제물에는 **패딩이 없다**(32 B 이상 0-run 이 헤더의 미사용 뱅크 칸 202 B 뿐). 표가 안 가리키는 구간도 비어 있는 게 아니라 실제 대사 레코드로 차 있다(표 밖에서 순차 주사되거나 BATTLE 쪽 표가 가리키는 것으로 보인다). | 자리에서 늘리는 길이 없다. 한국어는 글자당 2바이트라 대사 3,687 B 가 약 5,200 B 가 되므로 **파일을 키워야 한다**(전체가 u16 주소라 상한은 64 KiB, 여유 10,183 B 로 충분). | 남은 미지수는 하나 — 게임이 이 파일을 푸는 RAM 목적지와 그 뒤 여유. `load_file(32, …)` 호출부를 EXE·MAP·BATTLE 정적 탐색으로는 못 찾았다(비동기 루틴 `0x800C2D44` 경유로 추정). 에뮬레이터에서 해제물 위치를 한 번 재면 끝난다. | 0-run 실측 202 B / 64 KiB 여유 10,183 B | open |
| 2026-08-30 | mbankb-cannot-grow | 실기 RAM 덤프(사용자가 PCSX-Redux Lua 로 2 MB 를 떠 줌)로 확정했다. `M_BANKB` 해제물은 RAM **0x80107B8C..0x801153C4** 에 55,353 B 그대로 올라온다. 그 자리는 `BATTLE.LZB` 오버레이(0x80106380) 이미지 안의 **구멍**(BATTLE 오프셋 0x1700..0x14600, 77,568 B)이고, 구멍은 `[268 B 포인터표][M_BANKB 55,353][런타임 생성 표 21,947]` 로 **빈틈 없이** 차 있다. 꼬리 21,947 B 는 어느 파일에도 없고 RAM 안에서도 유일 — 게임이 전투 중 만드는 작업 데이터다. | 파일을 키우는 길이 없다. 앞뒤 어느 쪽으로도 여유가 0 이다. 해제물 안에도 32 B 이상 0-run 이 헤더의 미사용 뱅크 칸 202 B 뿐이고, 바이트 동일 중복 레코드도 0 건이라 안에서 짜낼 것도 없다. 저뱅크(1바이트·반각) 빈 칸도 4칸뿐(그중 하나는 공백)이라 한글을 1바이트로 넣는 길도 막혔다. | **자리에서만 바꾼다** — 역문을 원본 레코드 길이 이하로 쓰고 남는 자리는 종단 FF 앞에 공백 글리프로 메운다(`tools/build_mbankb_ko.py`). 포인터를 하나도 안 건드리므로 표·주소 계산이 전부 그대로다. 대사 170건 중 **127건**이 예산 안에 들어갔다. 못 들어간 43건은 전부 무기명 컷인(`마징가블레이드` = 17 B vs 예산 12 B) — 한글이 글자당 2 B 라 카타카나 대비 1.5배가 되어 구조적으로 불가능하다. | 해제물 55,353 B 불변 / 재압축 34,577 B ≤ 17섹터 예산 34,816 / 왕복 일치 | open(무기명 43건) |
| 2026-08-30 | ram-probe-via-user-lua | emucap Mednafen 은 이 호스트에서 `python3` 가 Windows 스토어 스텁이라 런처가 프로세스를 못 띄웠고(심 필요), `SDL2.dll`(msys64/mingw64/bin)도 PATH 에 있어야 하며, 세션 토큰 파일 경로가 `EMUCAP_SESSION_TOKEN_FILE` 로 안 맞으면 handshake 가 hard fail 한다. | 세 가지를 다 넘겨도 게임을 전투까지 몰고 가는 비용이 크다. 사용자의 PCSX-Redux + Lua 워크플로가 훨씬 빠르다. | RAM 측정은 `D:/srw4s_ko/mbankb.lua` 를 사용자가 `dofile` 로 돌리는 방식으로 한다. 매 프레임 훅 `DrawImguiFrame()` 에서 `ffi.string(mem, 0x200000)` + `find` 로 서명을 찾고, 잡히면 RAM 2 MB 를 `ram.bin` 으로 떨군다. `PCSX.addBreakpoint` 콜백은 이번에 발화하지 않았다. | `ram.bin` 2,097,152 B 확보, 서명 106/109 조각 일치 | closed |
| 2026-08-30 | mbankb-not-reaching-game | 컷인 한글판 124건을 디스크에 넣었는데 게임은 계속 일본어를 그린다. **디스크는 확실히 한글판이다** — 배포 이미지 LBA 5043 을 섹터에서 직접 읽어 해제하면 한글판이고(`== 한글판: True`), 에뮬레이터 자신의 ISO 리더로 읽어도 한글판이며, `listDir` 이 보고하는 크기도 34,580 이다. 같은 디스크에서 STAYDAT·D_NAMES·M_BANKS 한글판은 전부 RAM 에 올라온다(실기 확인). 그런데 RAM `0x80107B8C` 의 55,353 B 는 **원본과 0바이트 차이**다. | 원본 바이트가 지금 디스크 어디에도 없는데(압축본 0회·해제본 0회, 트랙 2·3 포함, 내장 LZB 스트림 전수조사 포함) RAM 에 통째로 있다. 사용자 PC 재부팅 후에도 같다. | 배제한 것: PPF/IPS 자동패치 없음, 디스크 사본 1벌, `BATTLE.LZB` 안 내장본 없음, 파일 39개 raw·LZB(오프셋 0 및 내장 스트림) 전수조사에서 `M_BANKB.LZB` 외 일치 0. 코드에도 **파일 인덱스 32 를 부르는 곳이 없다**(EXE 는 0·31·35, MAP 은 1~5·16·21·28·36, BATTLE 은 파일 읽기 호출 자체가 없음). 실기 BP 로 잡은 것은 MAP 해제(`ra=0x800C1634`) 한 건과 STAYDAT 내장 스트림 해제(`src=0x800648D6 dst=0x80010000 ra=0x801155A4`) 한 건뿐. **PCSX-Redux 는 `Dynarec=false` + 디버거 활성 상태여야 BP 가 발화한다**(이걸 몰라 여러 번 헛돌았다). 다음에 이어갈 때는 **타이틀 화면에서** 훅을 걸고 전투까지 들어가 M_BANKB 해제 호출을 잡는 것부터 한다 — 지금까지는 전투 데이터가 이미 상주한 뒤에 훅을 걸어 아무것도 안 잡혔다. | `build/M_BANKB_ko.LZB` 34,580 B (17섹터 예산 34,816 안), 해제물 55,353 B 불변, 왕복 일치 | open |
| 2026-08-30 | mbanks-slot-5678 | H16·H17·H19 번역으로 M_BANKS 가 **321섹터**가 되어 `@5699` 자리(최대 317)를 넘었다. 앞의 작은 파일 `D_PILOT`(5) `D_ROBOT`(7) `D_UNIT`(3) `D_WEAPON`(6) 21섹터를 꼬리로 옮기고 `--place @5678` 로 시작을 앞당겼다. | 지금 자리(5678..6015)는 **최대 338섹터**. 꼬리 여유는 63 -> 42 로 줄었다. | 다음에 막히면 `D_PALET`(29) 를 옮겨 `@5649`(367섹터)까지 갈 수 있다. 그 뒤는 `D_OBJCT`(47) 인데 꼬리 여유(42)로는 안 들어가므로, 그때는 `D_NAMES`(26, `--relocate DAT/D_NAMES.BIN=build/D_NAMES_ko.BIN`) 를 함께 검토한다. | `--place` 검증 'ISO가 LBA 5678 크기 655491로 보고, 내용일치=True' / 꼬리 여유 42섹터 | open |
| 2026-08-30 | inner-alias-cluster-h19 | H19 의 `{B:1E80}「...」` 변형 응답 풀(0x30611~0x30854)은 **안쪽 별칭이 촘촘히 박힌 구간**이다. 번역했더니 재뱅킹 검증이 슬롯 18개를 '설명 안 됨'으로 잡았다 — 표 슬롯들이 레코드 **중간**(예: `{B:1E80}` 토큰의 두 번째 인수 바이트)을 가리키고 있어서, 역문으로 길이가 달라지면 그 별칭이 엉뚱한 곳을 읽는다. | `mbanks-reach-and-alias` 와 같은 부류다. 표식 짝 검사·조판 검사는 전부 통과하므로 **재뱅킹 검증만이 방어선**이다. | 해당 8건(`MB:30609` `3062B` `30643` `30659` `30706` `307D5` `307ED` `3084D`)만 원문으로 되돌리니 통과했다. 구간 전체를 버릴 필요는 없었다 — 실패 오프셋을 담은 레코드만 정확히 골라내면 된다. | `verify_mb_rebank4_tr.py` 설명 안 됨 18 -> 0 | closed |
| 2026-08-30 | window-contract-per-table | H50(유닛 도감)을 번역했더니 조판 게이트가 31건을 '4줄>3' 으로 잡았다. 원문을 재 보니 도감은 **대사창이 아니다** — 실측 최대 **440px · 7줄**(대사창은 296px · 3줄)이다. | 296px/3줄 하나로 모든 창을 재고 있었다. 그대로 두면 멀쩡한 역문을 계속 깎게 되고, 반대로 넓은 창에서 진짜 넘치는 것을 못 잡는다. | `mb_layout` 에 `WIDE_PX=440` `WIDE_LINES=7` 과 `budget()` 을 넣고 `violations`·`rewrap`·`_split_line` 에 폭 인자를 뚫었다. 판정은 **레코드가 아니라 표 단위**로 한다 — 레코드 하나만 보면 같은 도감 안에서 3줄에 들어가는 짧은 설명이 대사창으로 잘못 잡힌다(37건이 그랬다). 넓은 표는 눈으로 확인해 고정: `H48` `H49` `H50` `H51`(캐릭터·유닛 도감). | `verify_mb_ledger` PASS / H50 236건 반영 | closed |
| 2026-08-30 | reflow-reads-batches-not-ledger | 폰트에 없는 `·` 를 원장에서 직접 고쳤는데 `reflow_ko --apply` 를 돌리자 되살아났다. 재조판 로더는 **배치 TSV** 를 정본으로 읽어 원장을 덮어쓴다. | 원장만 고치면 다음 재조판에서 조용히 되돌아간다. | 문자 치환 같은 일괄 수정은 `translation/batches/*.tsv` 에 한다. (`·` 와 `α` 는 글꼴에 없어 각각 `,` 와 `알파` 로 바꿨다.) | `undrawable` 0건 | closed |
| 2026-08-30 | bank-block-64kib-cap | H48(캐릭터 도감 212건)을 번역했더니 재뱅킹이 `표48 슬롯228 rel=65608 이 u16 범위 밖` 으로 죽었다. 뱅크 블록은 `[표 0x200][그 표가 읽는 구간]` 한 덩어리이고 **u16 상대 주소라 64 KiB 를 못 넘는다**. H48 블록은 68,723 B 로 3,187 B 초과였다. | 레코드 합만 보면 52,659 B 라 여유가 있어 보이지만, 재뱅커가 **별칭 접미사 사본**(922개 포인터)까지 같은 블록에 넣는다. 블록 크기는 레코드 합이 아니라 재뱅커의 배치를 그대로 흉내 내 재야 한다. | 긴 항목부터 마지막 문장을 덜어내 3,444 B 를 줄였다(52건). 도감은 설명문이라 끝 문장이 대개 부연이어서 손실이 적다. 표식(`{B:...}`)이 든 문장은 건너뛴다 — `MB:5830F` 이 그 검사에 걸려 손으로 다시 썼다. | `mb_rebank4 --tr` PASS / 블록 65,279 B | closed |
| 2026-08-30 | mbanks-slot-5649 | M_BANKS 가 343섹터가 되어 `@5678` 자리(최대 338)를 넘었다. `D_PALET`(29섹터)을 꼬리로 옮기고 `--place @5649` 로 앞당겼다. | 지금 자리(5649..6015)는 **최대 367섹터**. 꼬리 여유는 42 -> 13 으로 줄었다. | 다음 후보는 `D_OBJCT`(47)인데 꼬리 여유 13으로는 안 들어간다. 그때는 트랙1 꼬리 확장(별도 게이트)이나 `D_NAMES`(26, `--relocate ...=build/D_NAMES_ko.BIN`) 를 검토한다. | `--place` 검증 'ISO가 LBA 5649 크기 700711로 보고, 내용일치=True' / 꼬리 여유 13섹터 | open |
| 2026-09-02 | karaoke-translation-scope-and-schema-drift | 사용자가 `records/karaoke_lyrics.tsv` 전행 한국어 번역을 명시 요청해, 일본어 원곡과 맞추려고 가사를 원문 유지하던 기존 제품 방침을 이 파일 범위에서 변경했다. 작업 도중 다른 프로세스/세션이 TSV와 companion JSON을 `id, owner, bytes, max_ko, jp, ko` 스키마로 재생성해 첫 편집분이 사라졌다. | 최신 스키마의 `bytes`/`max_ko`가 고정 길이 소비자 제약을 드러내므로 장문 직역은 들어갈 수 없다. 이 TSV는 번역 초안으로 사용할 수 있지만 companion JSON·원장 병합·글리프/빌드/런타임 상태는 자동 승격하지 않는다. | 최신 TSV를 다시 읽고 `ko`만 118/118행 번역했다. ID·owner·bytes·max_ko·jp를 companion JSON과 전수 대조했고 불일치 0, 공란 0, 중복 ID 0, 일본어 잔존 0, 추정 인코딩 비용(`ASCII=1B`, 그 외=2B) 초과 0을 확인했다. JSON은 사용자 지정 범위 밖이라 수정하지 않았다. | `records/karaoke_lyrics.tsv` SHA-256 `B70618EC803628B028D88D8C31D585EB74EEAD2512207E97AC69D7186C0131A2`; `records/karaoke_lyrics.json` SHA-256 `356CE8BDC493E1E2C5357A716768E482CFB84B9177E87CD00EB212759F8242B0` | draft-complete_review-and-build-open |
| 2026-09-02 | karaoke-denominator-correction | 사용자가 가라오케 번역 누락을 보고했다. 최신 `make_karaoke_doc.py`는 TSV가 아니라 D_NAMES 원장의 S17 일본어 스팬을 직접 집계하며, 이전 118행 외에 `DN:C256`, `DN:C2B8`, `DN:C3C8`, `DN:C49B`, `DN:C5FD`, `DN:CA2C` 6행을 추가로 발견해 총 분모가 124행이 되었다. | 이전 118/118 완료 판정은 당시 TSV 분모에만 유효했고 실제 가라오케 전체 범위를 충족하지 못했다. TSV만 수정하면 다음 재생성 때 원장의 공란으로 되돌아간다. | 누락 6행을 번역하고 `apply_karaoke.py --apply`로 D_NAMES 원장에 동기화한 뒤 `make_karaoke_doc.py`로 TSV·JSON·HTML을 재생성했다. 실제 인코더 기준 124행 공란 0, 중복 0, 행별 `max_bytes` 초과 0, 일본어 잔존 0, TSV↔JSON↔원장 불일치 0이며 역문 1,807 B / 총 예산 3,891 B다. | `records/karaoke_lyrics.tsv` SHA-256 `5C00EAA13C47493EA37446B63699DF8BEA8471FC2DF1475D52A35D9DE0E3A824`; `translation/dnames_ledger.json` SHA-256 `710188F940C1DEBC9F2452A95E29B3ED1CA92D5A04F97652C7FC62E5CDF27D5C` | closed-static-build-runtime-open |
